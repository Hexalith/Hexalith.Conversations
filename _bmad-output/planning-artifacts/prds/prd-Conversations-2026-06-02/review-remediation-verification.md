# Remediation Verification — Conversations Boilerplate Reduction PRD

- **Verifies:** remediation of `validation-report.md` (2026-08-18T22:51) findings in `prd.md` (frontmatter `updated: 2026-08-18`) and `addendum.md`
- **Run at:** 2026-08-18 (post-edit working tree; both files modified/staged, edits not yet committed)
- **Method:** every severity finding checked against the actual new text; every artifact path cited in new text stat-checked on disk under repo root; load-bearing artifact contents (commits, counts, row ids, LOC, versions) opened and compared; all internal links and addendum anchors resolved programmatically; grep sweeps for stale phrasing; sprint-status and hold-proof claims cross-checked.

## Verdict

The remediation is genuine and near-complete: of the 22 severity findings (2 critical, 5 high, 9 medium, 6 low), 20 are fixed with verifiable new text and 2 are partially fixed (Adversarial C1 and H3), with no finding left unaddressed and no broken cross-reference introduced. The fixes are not cosmetic — every artifact path newly cited in the PRD exists on disk, and the load-bearing claims inside them check out: `release-baseline-v1.json` really binds commit `ceb7fbe9…` with 14 suites / 214 tests, `consume-promote-keep-inventory-v1.json` really carries 13,289 / 35,769 LOC with the exact split rows the addendum names (`publication-transport-marshaling` 422 LOC Promote FR-13 Story 3.5; `publication-failure-taxonomy` 131 LOC Keep), `sm-c2-hot-path-baseline-v1.json` really contains inventory `sm-c2-hot-path-inventory-v1` with the four HP-* rows and a reconstruction block with commit bindings and SHA-256 overlay hashes, `preservation-traceability-manifest-v2.json` really is `2.0.0-draft` / `pending-prerequisites` exactly as the PRD now discloses, and sprint status really shows Epics 1–6 done / 7–9 backlog with the implementation hold ACTIVE per the cited proof document. The two partials are narrow: C1's new reconstruction rule does not pin *which* frozen commit the SM-C2 baseline must bind (the artifact binds `29def441…` — the Story 6.7 done commit — not FR-20's frozen pre-refactor commit `ceb7fbe9…`), and H3's Contracts/Testing→Keep decision is now cited by artifact and rationale but still names no owner. Three low-grade residual tensions remain (§6.3 boundary-table FR-3 row, UJ-2's "single source of truth", md/json hash drift inside the SM-C2 sidecars). The new prose matches the document's register and table conventions throughout. This package would now defensibly re-grade well above Poor.

## Finding-by-finding disposition

| # | Finding | Disposition | Evidence |
|---|---|---|---|
| C1 | Adversarial C1 — post-hoc document masquerading as a plan; unbound baselines; no reconstruction rule | **Partially fixed** | All three prescribed elements present: §2 row "Execution status (2026-08-18)" and §5.3 closing paragraph (prd.md:31, 107) state Phases 0–3 executed as Epics 1–6 with hold ACTIVE (verified against `sprint-status.yaml` epics 1–6 `done`, 7–9 `backlog`, and `epic-6-completion-supersession-current-proof-v1.md` "Implementation hold: ACTIVE"); FR-20 now cites `release-baseline-v1.json` (2026-06-02, commit `ceb7fbe9`, `public-contract-shape-baseline-v1.json`, "14 conformance suites, 214 tests" — all verified inside the JSON, prd.md:322); SM-C2 cites `sm-c2-hot-path-baseline-v1.json` "(reconstructed 2026-07-31 under FR-20's reconstruction rule)" (prd.md:348); FR-20 ¶2 adds the retroactive-reconstruction rule requiring commit + gitlink binding, byte-identical hashed overlays, identical envelope, self-declaration (prd.md:324). **Remaining:** the rule says the artifact must bind "the frozen source commit", which in FR-20's context reads as `ceb7fbe9…` (2026-06-02) — but the cited SM-C2 artifact binds root commit `29def441…` (2026-07-27, the Story 6.7 *done* commit per the hold-proof doc); its pre-refactor character rests only on the baseline EventStore gitlink `b2d3402…`, and the PRD never surfaces or licenses that distinction. The artifact also still self-describes under the undefined story criterion "AC1" (mitigated: the PRD now carries its own rule and no longer references AC1). |
| C2 | Adversarial C2 — SM-C2 measurement altitude unspecified; §8 invariant unverified | **Fixed** | SM-C2 (prd.md:348) pins the boundary: "explicitly **in-process warm-path**: it proves closure-local regression only and cannot observe cross-process hops"; names the hot-path inventory `sm-c2-hot-path-inventory-v1` (HP-CREATE, HP-APPEND, HP-LIST, HP-OPEN — all four verified as rows in the frozen JSON); adds "an empty identification does not satisfy this gate." §8 Performance (prd.md:353) gives the cross-service invariant its own testable consequence: "verified by structural evidence per identified hot path (dependency/call-boundary conformance check or trace-based assertion recorded with the SM-C2 evidence)." |
| H1 | Adversarial H1 + Rubric — SM-3 unachievable inside declared scope | **Fixed** | SM-3 (prd.md:343) restated exactly as prescribed: canonical shared implementation + Conversations consumes it + "each surviving sibling-module copy (Folders, Projects, …) is registered as follow-on fleet-migration debt… their survival does not fail SM-3." Grep confirms "exactly one source of truth" is gone. Minor residual: UJ-2 (prd.md:56) still says "the shared implementation is the single source of truth" — see collateral R3. |
| H2 | Adversarial H2 — Promote area 8 (638 LOC) scope-homeless | **Fixed** | Addendum §C intro (addendum.md:49) resolves row 8 into accepted-inventory split rows: `publication-transport-marshaling` (422 LOC, **Promote, FR-13**, Story 3.5) + `publication-failure-taxonomy` (131 LOC, Keep). Both rows verified in `consume-promote-keep-inventory-v1.json` with exactly those classifications, FR, story, and LOC. The SM-1 denominator is reconciled by construction: it derives from that same accepted inventory (reconciliation block sums 35,769 exactly, unattributed remainder 0). |
| H3 | Adversarial H3 — SM-1 denominator shrunk ≈4.7k with no citable authority; no denominator protection | **Partially fixed** | Protection fully done: §2 preservation-gate row now states "The no-silent-denominator-reduction rule protects both the FR-20 test manifest and the SM-1 plumbing-LOC baseline" (prd.md:28), and SM-1 (prd.md:339) requires an append-only changeLog entry per `classification-change-procedure-v1.json` (exists on disk; the inventory's changeLog already carries 3 conforming entries) "with the same named approval discipline FR-20 demands." Decision citation mostly done: addendum §A (addendum.md:7) names the authority ("the accepted inventory itself"), the split rows, and the per-row source-boundary rationale. **Remaining:** the prescribed "(owner, rationale, artifact)" triple lacks the owner — neither §A nor the inventory JSON (no `acceptedBy` field, only `status: accepted` / `acceptedDate`) names who made the Contracts/Testing→Keep call. |
| H4 | Adversarial H4 — SM-2 at exactly 50.00% on low-confidence evidence; boundary undefined | **Fixed** | SM-2 (prd.md:340) cites `minimal-module-authoring-cost-sm2-baseline-v1.json` as freezing both "the boundary and the counterfactual pre-initiative construction rules… not reconstructible at attainment time" (artifact exists; contains `boundaryReference` and a `preInitiativeEquivalent` block that itself declares `measurementType: estimated`, `confidence: low`); figures relabeled "**provisional**… do not establish target attainment"; and the FR-19 artifact "must also state which single-file decisions, if reversed, would flip attainment" — the exact single-file-sensitivity requirement prescribed. |
| H5 | Adversarial H5 — FR-3/FR-10/FR-13 acceptance overlap | **Fixed** | FR-3's first consequence (prd.md:151) now reads: "FR-3's acceptance evidence is host adoption and operation continuity only, while removal of the Conversations-owned AppHost, Aspire, and ServiceDefaults artifacts is asserted and evidenced solely under FR-10 and FR-13 per the §6.3 crosswalk" — the prescribed strip, applied with an explicit single-owner assignment. Minor residual: the §6.3 boundary table's FR-3 row (prd.md:219) still says "Conversations removes its domain-agnostic host implementation" — see collateral R2. |
| M-a | Rubric Decision-readiness — point-in-time "target met" claims in a final document | **Fixed** | "target met" no longer appears anywhere in prd.md (grep-verified). SM-1: "As of 2026-07-14 the versioned report… records 70.43%, accepted… as a directional measurement, not a target-pass claim; that artifact and decision, not this sentence, are authoritative for attainment" (prd.md:339; `success-metric-report-and-attestation-v1.json` + its release-owner-decision sidecar exist on disk). SM-2 figures relabeled provisional (prd.md:340). |
| M-b | Rubric Done-ness — §14 ↔ FR-20 linkage unmapped | **Fixed** | New FR-20 consequence (prd.md:330): "records which Feature-FR/Feature-NFR each denominator test traces to, and lists §14 obligations with no covering denominator test as explicitly unguarded (traceability map: `preservation-traceability-manifest-v2.json`; its `2.0.0-draft` / pending-prerequisites status is itself a tracked gap owned by the release owner)." Disk-verified: the JSON's `manifestVersion` is `2.0.0-draft`, `status` is `pending-prerequisites`, 947 obligations — the PRD's disclosure is accurate, not aspirational. |
| M1 | Adversarial M1 + Rubric Shape-fit — Vision "about half" contradicts 37.15% baseline | **Fixed** | §1 (prd.md:16): "classifies 13,289 of them — 37.15% — as plumbing (the Discovery first pass estimated about half; the accepted FR-2-governed inventory in addendum §A is authoritative)" — 37.15% leads; "about half" survives only as labeled Discovery provenance, exactly as prescribed. |
| M2 | Adversarial M2 — CORE "all eight" journeys vs nine listed | **Fixed** | Glossary CORE (prd.md:74): "across all nine preserved acceptance journeys (§14.3)". §14.3 table has exactly nine actor rows (Maya, Atlas, Sarah, Diego, Marcus, Julian, Helen, Naomi, Daniel). No occurrence of "eight" journeys remains (grep-verified). |
| M3 | Adversarial M3 + Rubric — crosswalk wrong both directions; FR-15 evidence missing | **Fixed** | New §6.3 mapping (prd.md:209–213) verified row-by-row against the addendum: FR-10/FR-13 → §F rows 4/6 *and* §E rows 1/5 ✓; FR-11/FR-12/FR-14 → §E rows 2–4, 6 *and* §F rows 3, 5, 2 ✓; FR-15 → §C row 4 + §D ServiceDefaults row ✓, now explicitly labeled "a single-consumer promotion justified under R1's confirmed-Conversations-need branch" (the prescribed alternative to inventing need evidence). The mapping is no longer claimed exclusive; both directions are declared. |
| M4 | Adversarial M4 — §14.1 blanket disposition vacuous for process third | **Fixed** | §14.1 (prd.md:408) partitions preserved requirements into two named classes: behavioral/quality obligations (FR-20/SM-C1 denominator constraints) vs process/planning/release-procedure (Feature-NFR1–8, 30, 37) and accessibility validations (Feature-NFR69–75), which "cannot appear in a pre-refactor conformance run, are not FR-20 denominator constraints, and their absence from the frozen manifest is not a manifest gap" — closes the literal-reader inversion. |
| M5 | Adversarial M5 — greenfield revisit trigger fires after the irreversible action | **Fixed** | §13 (prd.md:398): release owner verifies production status "**before any further plumbing-only test removal** (the deletions this assumption licenses are otherwise irreversible) and re-verifies before any shared-package or external release" — the Phase-0 timing prescribed, sensibly adapted to the already-executed status, with the release-time re-check retained. |
| M6 | Adversarial M6 — FR-20 manifest completeness open ("etc.") | **Fixed** | Glossary conformance suite (prd.md:73) now defines "exactly the closed category list bound by FR-20" (seven categories, identical to FR-20's list). FR-20 consequence (prd.md:329): "The conformance-suite category list above is closed; adding or removing a category is a manifest change requiring the same named approval as a test removal," plus the versioned deletion ledger `removed-test-justification-ledger-reconciliation-v1.json` and `at-risk-test-register-v1.json` — both exist on disk. The only remaining "etc." in prd.md (§11 R3, "Folders/Projects/etc.") is unrelated prose, not the manifest list. |
| M7 | Adversarial M7 — "already-demonstrated" seam undefined | **Fixed** | §6.3 Notes (prd.md:282): "A seam counts as **already-demonstrated** only when it ships in a released technical-module version and is exercised by at least one existing consumer or SDK conformance test; any other consumption under this clause requires an OQ-1-style architect sign-off recorded against the consuming story" — both prescribed options, combined. |
| L-a | Rubric — FR-18 template validation names no mechanism | **Fixed** | FR-18 (prd.md:304): "a recorded step-by-step walkthrough mapping each template step to the post-refactor module, versioned at repo-root `docs/release-evidence/thin-authoring-template-validation-v1.md` and attached to pilot acceptance." File exists on disk (8,873 bytes, 2026-06-24). |
| L-b | Rubric — two Mayas | **Fixed** | §3.3 protagonists renamed Nadia (UJ-1), Sam (UJ-2), Priya (UJ-3); grep confirms the only remaining Maya is the legacy §14.3 actor (prd.md:425). |
| L1 | Adversarial L1 — SM-1 "Validates FR-3..FR-17" sweeps deferred FR-16 | **Fixed** | SM-1 now ends "Validates FR-3..FR-15, FR-17." (prd.md:339); no `FR-3..FR-17` remains (grep-verified). |
| L2 | Adversarial L2 — Vision overstates duplication evidence | **Fixed** | §1 (prd.md:16) now matches §E exactly: tenant-access handler "in Folders and Projects" (§E rows 2–3), service-defaults wrappers "in Folders, Memories, Tenants, and Parties" (§E row 1), typed-HttpClient "in Folders and Projects" (§E row 4), with "(evidence: addendum §E)". |
| L3 | Adversarial L3 — §E row 8 pre-empts OQ-1 with "Commons.Testing" | **Fixed** | §E row 8 (addendum.md:102): "shared test fixtures (candidate landing zone only — e.g. a Commons testing package or EventStore.Testing; the final zone is an OQ-1 architecture decision)" — candidate, not target. |
| L4 | Adversarial L4 — §C violates FR-1/FR-2; Feature-FR102 hard-codes Option A | **Fixed** | §C intro (addendum.md:49) marks the table "**superseded provenance**: the sole FR-1 inventory object is the accepted repo-root `docs/release-evidence/consume-promote-keep-inventory-v1.json`" and states dual-labeled rows "must not be read as the accepted inventory," resolving rows 8 and 11 into exactly-one-classification split rows (verified in the JSON: 26 areas, none dual-classified, LOC reconciles to 35,769 with zero remainder). Feature-FR102 (prd.md:589) carries the bracketed note: "'Option A v1 deal' is a historical May 2026 term; per §14.4, no option is currently selected." |

**Counts: 20 fixed, 2 partially fixed (C1, H3), 0 not addressed.**

### Mechanical notes from the report (secondary)

- SM-1 FR range — fixed (see L1). §6.3 exclusivity — fixed (see M3). §D attribution drift — fixed: `IDomainQueryHandler` now attributed "EventStore.DomainService + Client" with the DomainService/Client split spelled out, and `IDomainProjectionHandler` to EventStore.DomainService (addendum.md:76–77). Evidence paths — now uniformly prefixed "repo-root" in prd.md. Feature-NFR30 ambiguity — fixed with the bracketed "[here: the legacy feature PRD this baseline preserves, not this refactor PRD]" note (prd.md:640). §14.1 archive relative link — still resolves (`_bmad-output/archive/conversations-product-contract-2026-05-31.md` exists).
- **Not addressed (mechanical only, never a graded finding):** glossary drift — "plumbing" remains undefined while used interchangeably with the defined "boilerplate."

## Collateral damage and residuals

| ID | Severity | Description |
|---|---|---|
| R1 | **Medium** | **SM-C2 baseline's bound commit does not match the most natural reading of FR-20's new reconstruction rule.** FR-20 ¶2 requires a reconstructed baseline to "bind the frozen source commit and submodule gitlinks"; in FR-20's context "the frozen commit" reads as `ceb7fbe9…` (2026-06-02). The artifact the PRD now blesses ("reconstructed 2026-07-31 under FR-20's reconstruction rule", prd.md:348) actually binds root `sourceCommit` `29def441…` — dated 2026-07-27 and identified elsewhere as the Story 6.7 *done* commit — with pre-refactor character carried only by the baseline EventStore gitlink `b2d3402…` ("The production sources intentionally differ; that is what the regression gate measures"). A strict adversarial reader can argue the newly cited artifact fails the newly added rule, reopening a sliver of C1. Fix is one clause: state in FR-20 or SM-C2 which commit a reconstruction must bind for each artifact class, or acknowledge the SM-C2 baseline's per-surface frozen commit explicitly. |
| R2 | **Low** | §6.3 boundary table, FR-3 row (prd.md:219): "Conversations removes its domain-agnostic host implementation and runs through the platform host" still phrases FR-3's slice as a removal, in tension with FR-3's new consequence assigning removal evidence "solely under FR-10 and FR-13" (and with FR-13's "or equivalent runtime-host module" sweep). Reword the boundary cell to adoption-only phrasing. |
| R3 | **Low** | UJ-2 (prd.md:56) still ends "The Conversations copy disappears; the shared implementation is the single source of truth" — the phrasing H1 removed from SM-3 survives in the journey narrative, where sibling copies in Folders/Projects outlive the pilot. Add "for Conversations" or the fleet-migration qualifier. |
| R4 | **Info** (pre-existing, not caused by these edits) | Inside the cited SM-C2 evidence pair, the `.md` sidecar's "Fixture SHA-256: 4838a5a1…" disagrees with the authoritative JSON's `fixture/sha256: 1a43bacc…` (both dated 2026-07-31; the md defers to the JSON as authoritative). Worth reconciling before anyone audits the reconstruction chain the PRD now leans on. |
| R5 | **Info** (pre-existing) | The inventory JSON's own `baselineCommit` is `bf3d052` (Story 1.4 capture), distinct from FR-20's `ceb7fbe9`; the PRD never claims otherwise, but readers tracing "frozen baselines" will now meet three different frozen commits (`ceb7fbe9`, `bf3d052`, `29def441`) with no single sentence relating them. |

No broken internal cross-references were found: all 13 relative links in prd.md (including the four addendum anchor forms `#c-…`, `#d-…-fr-3fr-9`, `#e-…--shared-capability-candidates-fr-10fr-15`, `#f-…`) resolve against the addendum's actual heading slugs, and the §14.1 archive link resolves on disk. All ten spot-check artifact paths named in the task exist, plus `public-contract-shape-baseline-v1.json`, `success-metric-report-and-attestation-v1.json` (and its release-owner-decision sidecar), `classification-change-procedure-v1.json`, and `oq-2-target-interpretation-decision-v1.json`. No stale "target met", "about half" (outside labeled provenance), "eight journeys", "exactly one source of truth", §3.3 Maya, or "FR-3..FR-17" text survives.

## Prose and structure notes

- The new text holds the document's register: dense, evidence-citing, em-dash-joined qualifications, bolded key terms, identical table conventions (the §2 "Execution status" row and §5.3 execution-status paragraph mirror the existing decision-table and phasing styles). Nothing reads pasted-in.
- SM-1 and SM-2 have grown into ~150-word single bullets; they remain readable but are now the densest prose in §7 — consider sub-bullets if edited again. Same for the addendum §C intro paragraph, which does three jobs (supersession, row-8 resolution, row-11 resolution) in one breath.
- The remediation consistently chose "dated pointer + artifact is authoritative" phrasing over deleting the 2026-07-14 figures — the right call for a finalized document, and applied uniformly (SM-1, SM-2, §2, §5.3), so the document no longer contains any undated attainment claim.
- FR-20 is now the longest FR by far (two paragraphs + six consequences); it carries the reconstruction rule, the closed category list, the deletion ledger, and the traceability map coherently, but R1 above is the one seam left open in it.
