---
epic: 6
date: 2026-08-03
verdict: rejected
criteria: declared
headless: false
last_reconciled: 2026-08-16
---

# Epic 6 Retrospective

## Epic summary

- Current scope: Stories 6.1, 6.7, and 6.2, the immutable corrective-foundation spine declared by the v10 context. All three are `done` in sprint status.
- Pending-story gate: none. `sprint_status.py detect-epic --epic 6` reported `story_count: 3` and `pending_stories: []`.
- Delivery range: `f31aa5ada2e37e1ec5f3e4b8e907525b37da863f..e480c3f3176cdc3d911baf91eb3e7a8cd38874aa`, from Story 6.1's recorded pre-change baseline through Story 6.2's done-transition commit.
- Scope caveat: the delivery range contains 88 non-merge commits with interleaved Story 6.8 and BMAD synchronization work. The current `git_evidence.py` pre-pass reports 608 changed paths; the original retrospective run recorded 583 before later evidence-tool changes. Current authority classifies that partial Story 6.8 work as unaccepted input to Epic 7, not as an Epic 6 deliverable (`epics.md:2270-2283`; `epic-6-context.md:7,40`).
- Evidence available: all three story records, current v12 planning authority plus the additive V13 current-proof sidecar, Git history and raw tree entries, the Epic 5 retrospective, sprint action ledger, current source/tests, and executable .NET/Python verification. The original runtime verification remains recorded below.
- Evidence missing: no separate session/conversation logs were found. Process analysis therefore uses the unusually detailed review and completion records embedded in the story files and Git history.
- Working-tree boundary at the 2026-08-16 reconciliation: the pre-existing deletion of 91 comment lines from `sprint-status.yaml` was preserved. It affects current planning-validator results but does not alter Epic 6 story or action-item statuses. The reconciliation edits only this retrospective document.

## 2026-08-16 reconciliation

- The additive V13 evidence was accepted by the release owner for its expressly limited present-state question. Its decision binds evidence HEAD `52240cff72f1b8dc985c52a1a50f67de9800a79f`, preserves the historical V12 `FAIL` / `REJECTED` result, keeps the implementation hold active, and authorizes neither IR-0 nor release (`epic-6-completion-supersession-current-proof-decision-v1.json:22-52,61-75`).
- That human decision justifies the recorded `epic-6-retro-item-24` transition to `done`; it does not override this retrospective's Epic 6 acceptance verdict. The decision says the historical route remains rejected and superseded nothing (`epic-6-completion-supersession-current-proof-decision-v1.json:30-47,61-65`).
- The accepted evidence is no longer a description of the current tree. Commits `588381b`, `28f6fbb`, `4ba45a7`, and `1a7c08a` followed evidence HEAD `52240cff`; nine of the ten evidence-bound root gitlinks now have different object IDs. A fresh current-proof run at `1a7c08a` returns `FAIL`, not `PASS`.

## Findings

### Reconciliation findings

| Finding | Evidence | Disposition | Prevention lesson |
| --- | --- | --- | --- |
| **Blocking for any current-tree claim:** the accepted V13 proof is immutable point-in-time evidence, not an evergreen proof. It binds `52240cff`; current HEAD is `1a7c08a`, nine bound gitlinks changed, and the same declared command now reports 204 passed / 21 failed tests. | `epic-6-completion-supersession-current-proof-v1.json:49-60,93-143,151-170`; commits `588381b`, `28f6fbb`, `4ba45a7`, `1a7c08a`; live `verify_epic_6_completion_supersession.py --current-proof --execute-tests` result `E6_CURRENT_PROOF_TEST_FAILED`. | Keep the historical A1 transition as the release owner's point-in-time decision, but do not cite V13 as proof of the current candidate. Route current workflow drift through open A2/A3; any later candidate needs additive, versioned evidence rather than a rewrite of v1. | A proof named current must be re-evaluated at the candidate that consumes it, with exact evidence-head and gitlink equality. |
| Current-proof resolves a committed HEAD but loads its contract/schema and runs tests from the mutable worktree. No clean-tree check or exact-HEAD materialization prevents uncommitted bytes from being attributed to the resolved commit. | `_bmad/scripts/verify_epic_6_completion_supersession.py:540-546,609-666`; no dirty-worktree fault exists in `_bmad/scripts/tests/test_verify_epic_6_completion_supersession.py:251-470`. | Fix under A3 before relying on this route again. | Evidence labeled with a commit must execute only bytes from that commit, or visibly block on applicable worktree dirt. |
| The V13 authority validator hardcodes A1 to `done` and checks equality with its own rendering without consuming the current-proof evidence or independent decision. The required preflight does not run the V13 current-proof command or a decision-chain validator. | `_bmad/scripts/publish_v13_current_proof_authority.py:74-150,154-185`; `_bmad/scripts/tests/test_publish_v13_current_proof_authority.py:27-72`; `.github/workflows/planning-authority-preflight.yml:48-68`. | Fix under A3. The current `publish_v13_current_proof_authority.py --check` green result is authority-shape evidence only. | A completion sidecar must derive status from validated evidence and decision bindings, not encode the desired status as a constant. |
| Post-done changed paths are an endpoint diff and their test oracle calls the same helper. A path changed and later reverted disappears, while a helper regression can change observed and expected lists together. | `_bmad/scripts/verify_epic_6_completion_supersession.py:595-598,638-646`; `_bmad/scripts/tests/test_verify_epic_6_completion_supersession.py:264-273`. | Fix under A3 with an independent raw-Git fixture and omission/unexpected-path faults. | Claims about every path changed over an interval require history-union semantics or wording limited to endpoint differences, plus an independent exact-set oracle. |
| Current-proof checks only the ten paths declared by its mutable contract. It neither derives the complete root mode-`160000` inventory from `.gitmodules`/HEAD nor proves each recorded object exists in the corresponding local submodule repository. | `_bmad/schemas/epic-6-completion-supersession-current-proof-v1.schema.json:50-55,104-115`; `_bmad/scripts/verify_epic_6_completion_supersession.py:575-592`; `epic-6-completion-supersession-current-proof-contract-v1.json:17-27`. | Fix under A3 before the route is reused. | Root gitlink evidence needs exact inventory equality and object availability, not only mode checks for a declared subset. |
| The V13 command surface is tooling-only and its skip parser misses pytest's normal count-first form such as `1 skipped`; it also freezes no test identities/count. This evidence cannot substitute for Story 6.2's .NET/runtime lane or Story 6.7's exact promotion lane. | `epic-6-completion-supersession-current-proof-contract-v1.json:33-37`; historical contract commands at `epic-6-completion-supersession-contract-v1.json:49-54,82-96`; `_bmad/scripts/verify_epic_6_completion_supersession.py:270-299`; current-proof schema `:66-69,116-126`. | Treat V13 as tooling-health evidence only. Fix parsing/inventory under A3 and keep the original historical acceptance failure intact. | Machine-readable result artifacts and frozen test identities prevent deselection, skips, or a narrowed lane from reading as complete verification. |
| A3's original vacuous zero-parameter predicate and historical checkout fallback are fixed, but the replacement consumer-callability check still accepts any public type anywhere in the file rather than the matched method's declaring type. | `ArchitecturePlanningAuthorityValidationTest.cs:1199-1231`; `verify_evidence_boundary.py:303-321`. | Keep A3 open and fix with containing-type parsing or a compiled consumer fixture. | Structural signature checks must bind the exact member to its declaring type; nearby public syntax is not proof of consumer reachability. |

### Aggregate architecture and change-shape view

| Finding | Evidence | Disposition | Prevention lesson |
| --- | --- | --- | --- |
| The v10 rebaseline now states a coherent three-story Epic 6 boundary and transfers each unfinished former story exactly once to Epics 7-15. | `epic-6-context.md:5-24,38-40`; `epics.md:2270-2289`; `epic-6-current-execution-view-v2.md:23-57`; current planning validators 5/5 and historical architecture validators 17/17 green. | Accept as-is. The scope reconciliation is internally consistent and does not lift the implementation hold. | Derive retrospective scope from current authority, not from every commit or artifact carrying an old Epic 6 identifier. |
| The broad Git range is unsuitable as an exact ownership ledger: 34 commits name Story 6.2, 54 do not name any supplied Epic 6 story id, and partial Story 6.8 work is interleaved. | `git_evidence.py` over the declared range; Story 6.2 itself records the shared-workspace authorship partition at `6-2-migrate-conversations-to-platform-owned-hosting.md:1769-1776`. | Accept as provenance limitation; do not use range totals as acceptance proof. | Freeze and verify each story's candidate and exact path set. Use story-scoped Conventional Commit subjects where practical; 85/88 subjects matched the repository convention, but three did not. |
| Platform-hosting ownership moved in the intended direction: local ServiceDefaults projects were removed, the domain host uses the public EventStore composition pair, and the retained AppHost remains a non-shipping test harness. | Project/solution diff; `src/Hexalith.Conversations.Server/Program.cs:14-44`; `epics.md:1580-1647`; live AppHost check below. | Accept as-is. | Keep production topology and reusable hosting capability outside the Conversations module; preserve the retained AppHost's non-packable/non-publishable guards. |
| The largest new implementation artifacts are concentrated in evidence tooling and high-fidelity tests: `generate_story_record.py` 2,752 lines, promotion tests 1,685, generator tests 1,474, authority validation 1,732, and the projection-handler test 1,271. | Current line counts and diff churn. | Defer the generator/tooling concentration to Epic 7, where current authority already treats Story 6.8's partial implementation as unaccepted input. The test concentration is a maintainability risk, not an Epic 6 acceptance failure by itself. | Split executable contracts by stable responsibility while preserving exact assertion identities and non-vacuity. |

### Cross-story dependency and completion evidence

| Finding | Evidence | Disposition | Prevention lesson |
| --- | --- | --- | --- |
| **Blocking:** Story 6.7's verified candidate was not final. Its record says the live gate passed at candidate `aa2b6b7` and then moved the story to `done`, but commit `29def441` both performs the done transition and changes the checker, its tests, both code-review workflow files, and the EventStore and Memories gitlinks. No post-commit evidence in the record binds that tree. | `6-7-mechanically-block-incomplete-submodule-promotions-from-completion.md:7-8,642`; `git show --raw 29def441408becfbbbdc5c59b9af14a7717cb21f`. | Fix now through additive successor evidence; do not rewrite the completed record. | The done-transition commit may change only declared record/status outputs. Any source, workflow, test, or gitlink movement requires a new committed candidate and rerun before `done`. |
| **Blocking:** Story 6.2's generated final record binds candidate `2971ab79`, says no paths or gitlinks moved afterward, and contains test hashes for that candidate. Its done-transition commit `e480c3f3` then moves seven root gitlinks, including all three declared promotions: Builds, EventStore, and Tenants. | `6-2-migrate-conversations-to-platform-owned-hosting.md:6-15,1780-1784,1931-1950`; raw tree diff for `e480c3f3176cdc3d911baf91eb3e7a8cd38874aa`. | Fix now through an additive supersession/audit record. This invalidates Story 6.2 AC3/AC7 and Story 6.7 AC5 at the acceptance boundary. | Finality must be checked against the actual done commit, including raw mode-`160000` entries, rather than against its parent. |
| The current historical verifier returns `pass` with zero blockers for Story 6.2 even though it never checks the done commit's post-candidate changes. | Live `generate_story_record.py --historical` result; implementation at `_bmad/scripts/generate_story_record.py:1983-2085`; existing ledger entry at `deferred-work.md:200-210`. | Fix now via planned Epic 7 historical verification; keep the hold active. | Historical verification must resolve the recorded candidate, done/finalization commit, protected blobs, and gitlinks and fail closed when any is unavailable or moved. |
| At the original review, workflow adoption had drifted away from Story 6.7. The then-current promotion suite was 72 passed / 13 failed. The 2026-08-16 reconciliation confirms the defect recurred after BMAD 6.11: active lifecycle gate blocks were removed in commit `4ba45a7`, and the complete Python lane is now 204 passed / 21 failed. | Original `pytest _bmad/scripts/tests/test_verify_submodule_promotion.py -q`; current failure paths in `test_generate_story_record.py`, `test_verify_evidence_boundary.py`, and `test_verify_submodule_promotion.py`; commit `4ba45a7`. | Keep A2 open. No promotion-bearing completion should proceed before active-route gates and their complete verifier lane are green. | Bind gate coverage to the active route inventory and require the verifier after every BMAD synchronization. |

### Adversarial and edge-case lens

| Finding | Evidence | Disposition | Prevention lesson |
| --- | --- | --- | --- |
| The Story 6.1 platform-surface validator can fall back from an unavailable recorded gitlink/blob to the current checkout, substituting mutable worktree evidence for candidate-bound evidence. | `ArchitecturePlanningAuthorityValidationTest.cs:1244-1262`. | Fix now. | Missing candidate history is `BLOCKED`; current bytes are never a substitute for historical bytes. |
| The same validator's empty-fragment branch checks `Trim().Length >= 0`, so its promised non-empty parameter guard is vacuous. | `ArchitecturePlanningAuthorityValidationTest.cs:1199-1216`. | Fix now. | Mutation-test the empty/zero-parameter distinction and exact consumer-callable signature. |
| An uninitialized or broken unrelated submodule worktree can leave the promotion checker with no warning; current `inspect_unrelated` returns immediately when `own_worktree` is false. | `_bmad/scripts/verify_submodule_promotion.py:622-642`; independently reproduced by the verification-gap review. | Fix with the current-route gate repair. | Distinguish a proven clean unrelated worktree from an uninspectable one using a stable warning code. |
| `require_remote` proves only that a locally known remote-tracking ref contains the commit. | Story 6.7 AC2d at `6-7-mechanically-block-incomplete-submodule-promotions-from-completion.md:57-63`; checker at `_bmad/scripts/verify_submodule_promotion.py:469-478`. | Accept as-is. The story explicitly says "locally known" and forbids fetches; no current-remote claim may be made from this result. | Name local-cache evidence precisely and obtain separately authorized remote freshness evidence only when required. |
| Replay timestamps are not deterministic when events lack usable timestamps; wall-clock `projectionGeneratedAt` also leaks into participant/file evidence timestamps. | `ConversationProjectionMaterializer.cs:115-134,318-329,376-388`; accepted disclosure in `6-2-migrate-conversations-to-platform-owned-hosting.md:1130,1133-1136`; `deferred-work.md:170-176`. | Defer to approved successor work, but retain as a high production-correctness item. | Replay-equivalence criteria must state structural versus exact fields; exact domain timestamps require immutable event-derived inputs. |
| Tenant access remains backed by the default in-memory projection, so restart or a second replica loses acknowledged tenant state and fails closed for legitimate callers. | `Program.cs:35-43`; `_bmad-output/project-context.md:53-57`; `deferred-work.md:176`. | Defer to approved successor work. The disclosed deployment boundary remains single-replica-only. | Require restart and multi-replica convergence proof before removing the constraint. |
| A missing tenant index is indistinguishable from a never-used tenant and returns an empty `Current` page even if derived state was erased. | `ConversationProjectionReadStore.cs:146-159`. | Defer to projection-correctness successor work. | Persist an independent initialization/watermark fact or return a non-current state when prior use cannot be disproved. |
| Every tenant's summary/dispatch index is one sorted CAS-updated value with no page, shard, or explicit size limit. | `ConversationProjectionIndexReadModel.cs:10-32`; `ConversationProjectionReadModelWriter.cs:95-122,175-235`. | Defer pending a measured tenant-cardinality/storage envelope; no current scale evidence proves a failure. | Add a bound before this topology is approved for workloads that can exceed one state-store item. |
| The retained boundary lane can race endpoint readiness under load, and live lanes conflict with another Aspire application's fixed Dapr control-plane ports without a targeted diagnostic. | `6-2-migrate-conversations-to-platform-owned-hosting.md:1267-1270`; `deferred-work.md:178-198`. | Defer to the test/AppHost owner; today's isolated run passed. | Preflight fixed ports and distinguish endpoint-connect readiness from resource-health readiness. |
| The AppHost test proves command submission through the gateway but queries the module route directly because the gateway lacks a Conversations `DomainServiceOptions` registration. | `ConversationsAppHostRuntimeBoundaryTest.cs:196-207`. | Accept for Story 6.2: its declared production-query criterion does not require the gateway query route. Record as a future adopter-path decision, not as completed functionality. | State the exact ingress surface in future end-to-end criteria. |

### Verification-gap lens

| Finding | Evidence | Disposition | Prevention lesson |
| --- | --- | --- | --- |
| The planning-authority suites are operator-invoked; the repository has no root CI workflow that guarantees they run when authority/context/status artifacts change. | No files under root `.github/workflows`; executable guards in `ArchitecturePlanningAuthorityValidationTest` and `PlanningAuthorityV9ValidationTest`. | Fix with the planning-verifier hardening action. | Make authority changes trigger a required, non-vacuous preflight. |
| Conversations' terminal reconciliation is tested by calling the concrete handler directly, not by composing it through the production `project/v2/reconcile` route. | `ConversationAsyncProjectionHandlerTest.cs:168,194,224`; production route consumer in EventStore `NamedProjectionDispatchCoordinator.cs:726-750`. | Defer to runtime verification work. | Exercise the real coordinator-to-module route and prove durable retry state clears. |
| The current v10 planning publication is mechanically consistent despite the above delivery defects. | Live `PlanningAuthorityV9ValidationTest` 5/5 and `ArchitecturePlanningAuthorityValidationTest` 17/17. | Accept as planning evidence only. It neither repairs historical completion nor lifts the hold (`epic-6-current-execution-view-v2.md:15-17,53-57`). | Keep planning-publication validity distinct from implementation acceptance. |

## Behavior verification

- `aspire doctor` completed successfully except for the local HTTPS certificate trust warning; Aspire CLI version was 13.4.6.
- A managed isolated AppHost run with Keycloak disabled built and started Conversations, EventStore, their Dapr sidecars, and the admin web resource. Aspire reported the runtime resources healthy.
- Conversations `/health` returned HTTP 200 `Healthy`. EventStore initially returned 503 during Dapr startup, then converged to HTTP 200 `Degraded`: self, sidecar, actor placement, state store, pub/sub, and projection-writer protocol were healthy; only the absent optional Dapr configuration-store component was degraded, with application settings used as fallback.
- The focused AppHost test project built in Release with 0 warnings and 0 errors.
- `RetainedAppHostShouldRunEventStoreAndConversationsProductionBoundary` passed 1/1, with 0 failed/skipped/not-run, in 94.95 seconds. It exercised real command submission, terminal persistence, tenant-event consumption, durable projection writes, and query observation.
- The Conformance project built in Release with 0 warnings and 0 errors. `PlanningAuthorityV9ValidationTest` passed 5/5 and `ArchitecturePlanningAuthorityValidationTest` passed 17/17.
- The Story 6.7 Python suite failed overall: 72 passed and 13 failed. Failures are concentrated in stale/missing active workflow gate paths, not the isolated Git-fixture core.
- Story 6.2 historical mode returned `pass` with no blockers or warnings for candidate `2971ab79`; raw Git evidence proves this is a false green because `e480c3f3` changed seven gitlinks after that candidate.
- The AppHost started for this verification was stopped; `aspire ps --format json` returned an empty list afterward.

### 2026-08-16 reconciliation verification

- No product source under `src/` changed after the original retrospective; post-retro runtime behavior was therefore not relaunched. The original end-to-end AppHost observation above remains the runtime evidence, while the current dependency/tooling state was checked separately.
- `dotnet build tests/Hexalith.Conversations.Conformance.Tests/Hexalith.Conversations.Conformance.Tests.csproj --configuration Release -m:1` succeeded with 0 warnings and 0 errors against the current root gitlinks.
- Direct xUnit v3 execution passed `ArchitecturePlanningAuthorityValidationTest` 17/17. `PlanningAuthorityV9ValidationTest` failed 2/7 and `PlanningAuthorityV8ValidationTest` failed 1/6; one V9 failure is candidate-source digest drift, and the other V9/V8 failures observe the pre-existing working-tree deletion of sprint-status authority comments.
- `uv run --frozen python3 -m pytest -q _bmad/scripts/tests` returned 204 passed / 21 failed. The failures reproduce missing active lifecycle gates and V9 candidate-source drift after the BMAD 6.11 update.
- `publish_v13_current_proof_authority.py --check` passed, demonstrating only that the sidecar matches its hardcoded schema/rendering. `publish_v9_planning_authority.py --check` failed closed with `CANDIDATE_SOURCE_DRIFT: .agents/skills/bmad-build/compile-epic-context.md`.
- A fresh `verify_epic_6_completion_supersession.py --current-proof --execute-tests` returned `FAIL` with `E6_CURRENT_PROOF_TEST_FAILED` and retained `implementationHold: ACTIVE` / `releaseAuthorized: false`.

## Previous-retro follow-through

The original retrospective changed no earlier-epic action-item status. The later Epic 6 release-owner transition is reconciled separately below; the earlier-epic statuses remain unchanged.

| Selector required by sprint-status | Evidence and assessment | Proposed status |
| --- | --- | --- |
| Epic `3` + exact action `Make final story record generation mechanical from final test counts, file list, submodule state, and root gitlink state.` | Partial generator work exists, but current authority classifies it as unaccepted Epic 7 input; Stories 7.1-7.4 remain backlog behind the active hold. | Keep `in-progress`; no transition proposed. |
| Epic `5` + exact action `Promote the Story 5.3 evidence-boundary validation pattern into reusable dev/review guidance.` | The canonical runbook/customizations and v10 planning bindings exist, but authority explicitly keeps this action open until a compatible Story 10.4 `9/9/0/0/0/0` final record passes (`epics.md:4143-4156`). | Keep `open`; no transition proposed. |
| Epic `5` + exact action `Keep release-facing documentation aligned with Epic 5 attestation state without claiming approval.` | The root README makes no approval/signature claim, but the release decision remains open and the implementation hold remains active, so there is no evidence that this continuing release-governance obligation is complete. | Keep `open`; no transition proposed. |

### Epic 6 action-item reconciliation

| Selector | Current evidence | Status disposition |
| --- | --- | --- |
| `epic-6-retro-item-24-produce-an-additive-epic-6-completion-su` | Release-owner decision `epic-6-completion-supersession-current-proof-decision-v1.json:67-75` explicitly records the human-applied transition. | Keep `done`, scoped to the accepted point-in-time present-state decision. It does not change the Epic verdict. |
| `epic-6-retro-item-25-restore-the-submodule-promotion-and-evid` | The active verifier lane is 204 passed / 21 failed after commit `4ba45a7` removed current route gate blocks. | Keep `open`. |
| `epic-6-retro-item-26-harden-planning-authority-verification-t` | The original zero-parameter and historical-fallback defects are repaired, but the V13 decision chain is not consumed, the current publisher is candidate-drift blocked, and the declaring-type guard remains incomplete. | Keep `open`; no transition proposed. |
| `epic-6-retro-item-27-create-approved-successor-work-for-a-dur`, `epic-6-retro-item-28-create-approved-successor-work-for-deter`, `epic-6-retro-item-29-add-explicit-preflight-diagnostics-for-a` | V12 authority explicitly preserves A4-A6 as open work requiring separately approved successors (`architecture.md:2348-2356`; `epics.md:4318-4333`). | Keep `open`. |

## Action items

These are proposed remediation/process items. They do not edit completed story records, change planning authority, lift the hold, or implement product behavior in this retrospective.

The reconciliation adds no duplicate sprint item. Its new current-proof defects route to the already-open A2/A3 rows; A1's human-applied point-in-time transition is preserved.

| # | Action | Owner | Done when |
| --- | --- | --- | --- |
| A1 | Produce an additive Epic 6 completion-supersession record that binds the actual Story 6.7 and Story 6.2 done commits, every post-candidate changed path, and all ten root gitlinks; keep the global hold active until it passes. | Dev workflow / Release owner | The immutable old records remain unchanged; the new machine record either proves the exact completion trees or emits stable failure/blocker codes, and independent review accepts the result. |
| A2 | Restore the submodule-promotion and evidence-boundary gates on every current review/done route and make the Story 6.7 workflow-contract suite pass against the active BMAD surfaces. | Dev workflow | Active route inventory is exact, every completion transition is mutation-tested, and the suite is non-vacuous with zero failures. |
| A3 | Harden planning-authority verification to fail closed when candidate or recorded submodule evidence is unavailable, fix the vacuous parameter-signature predicate, and add mutation tests plus an automatic preflight. | Architecture / Quality | Missing history cannot fall back to current bytes, empty/wrong signatures fail, fault injections turn red, and authority changes invoke the focused preflight. |
| A4 | Create approved successor work for a durable event-fed tenant-access projection with freshness, gap detection, restart, and multi-replica convergence proof before scaling beyond one replica. | Architect / Runtime owner | Restart and two-replica tests converge from durable state without event redelivery and the single-replica warning is removed only after proof. |
| A5 | Create approved successor work for deterministic event-derived replay timestamps and trustworthy missing-index semantics, with exact replay and derived-state-deletion tests. | Projection owner | Identical history reproduces exact domain/freshness timestamps, and erased prior state cannot return an authoritative empty `Current` result. |
| A6 | Add explicit preflight diagnostics for AppHost endpoint readiness and Dapr control-plane port collisions, and cover the Conversations terminal reconciliation route end to end. | Test / AppHost owner | The two known environment signatures are identified directly, and a live terminal retry clears durable work through `project/v2/reconcile`. |

## Acceptance verdict

**Rejected — declared criteria.** All three current Epic 6 stories are marked done and no story is pending, but blocking acceptance evidence does not hold:

1. Story 6.7's done-transition commit changed the gate implementation, tests, workflow files, and two gitlinks after the last recorded candidate verification.
2. Story 6.2's done-transition commit changed seven gitlinks after its generated record claimed there were no post-candidate path/gitlink changes; all three declared promotion gitlinks moved without candidate-bound test or promotion evidence.
3. At the original review, the historical verifier falsely returned `pass` for that Story 6.2 record while the workflow-contract suite was red 13/85.

At the original review, the live runtime path and planning publication were green, but that success could not retroactively validate different dependency trees at the acceptance commits. The implementation hold therefore remains appropriate. Acceptance can be reconsidered only from additive, candidate-bound successor evidence and independent release-owner review; this retrospective does not rewrite immutable history or change any story status.

**2026-08-16 reconciliation: verdict unchanged.** The V13 release-owner decision is accepted as the human decision for its narrow present-state action-item question, but it explicitly preserves and does not supersede the V12 historical `FAIL` / `REJECTED` evidence. The accepted proof is now stale relative to current HEAD, and a fresh execution is red. No human decision overrides the Epic-level retrospective verdict, so the machine and recorded verdict remain `rejected`.

## Open questions

- Should the current-proof route gain a versioned release-candidate successor plus a required preflight consumer, or remain explicitly point-in-time tooling evidence that cannot gate a later candidate?
- What tenant-cardinality and state-store item-size envelope must the current single-key tenant index support before sharding/paging becomes a release criterion?
- Is the direct module query route the intended retained AppHost proof boundary, or should a future adopter-path story register Conversations with the gateway query route as well?
