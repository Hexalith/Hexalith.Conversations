---
title: 'Publish a successor Story 7.1 entry authority'
type: 'bugfix'
created: '2026-09-20'
status: 'in-review'
baseline_commit: 'e0b098fa1c056385e28ee8ac0efd0c55dfab324f'
route: 'dispatch'
review_loop_iteration: 1
context:
  - '{project-root}/docs/runbooks/evidence-boundary-validation.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-7-context.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** At `dcba5d4b1314eb67a95fa560b7cc0f88a9ab2607`, the V22 resolver returns `FAIL / CANDIDATE_GRAPH_DRIFT`: protected `main` is a two-parent merge, but V22 accepts only its historical direct-child transaction and hard-codes `executionAllowed: false`.

**Approach:** Pre-land successor-aware tooling while preserving V22, then generate an exact request that fails closed on missing gates. Stop for authenticated human approval; only a separate validated decision publication may return `PASS` with Story 7.1 `executionAllowed: true`.

## Boundaries & Constraints

**Always:** Derive commits, trees, path/mode sets, digests, raw mode-`160000` gitlinks, inventories, and ledgers from committed objects. Bind historical V22 candidate `cf82f8008d02b07d48338a545909d97faa302362`, the exact current merge/tree/gitlinks, Story 6.2, `7.1-SCHEMAS`, IR-0, V19/V20, Story 7.1 path inventories, the operational envelope, and current FR-20/SM-C1, SM-C2, and OQ-1 dispositions. Keep PASS, FAIL, BLOCKED, and applicable `not-applicable` distinct and nonvacuous.

**Never:** Weaken V22; copy or prefill owner approval; combine resolver tooling with the decision; rewrite V1-V22; edit Story 7.1 files, final records, submodules, gitlinks, dependencies, sprint state, release state, or push state; authorize Story done, Story 7.2, release, push, or a missing gate.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|---------------------------|----------------|
| Current HEAD | Two-parent `dcba5d4b1314eb67a95fa560b7cc0f88a9ab2607` under V22 | `FAIL`, `CANDIDATE_GRAPH_DRIFT`, ACTIVE/false | Preserve exact historical result |
| Technical request | Pre-landed tooling and committed evidence | Exact candidate request; execution false | Missing approval/gates are `BLOCKED` |
| Approved publication | Exact request, gates, and trusted decision | `PASS`, all-PASS ledger, Story 7.1 execution true | Drift is FAIL or BLOCKED/false |

</frozen-after-approval>

## Code Map

- `.github/workflows/planning-authority-preflight.yml` -- keep the protected-base host isolated; explicitly support fork-head checkout without executing candidate bytes, run bootstrap and hosts under an allowlisted isolated environment with the synchronized project virtual-environment interpreter, and prove the candidate checkout equals the event-selected SHA before candidate tooling runs.
- `_bmad/schemas/v23-story-7.1-entry-authority-v1.schema.json` -- preserve every exact fixed array, all 29 pinned authority source-binding paths, and index-referenced generic failures; give both protected hosts an external digest for these exact schema bytes rather than treating candidate schema relaxation as authority.
- `_bmad/scripts/publish_story_7_1_entry_authority.py` -- retain committed-object validation and deterministic request generation; independently enforce request/authority control fields, current-snapshot successor grants, runbook-bound recovery summaries, strict numeric thresholds, and cleanup/diagnostics for every post-create publication fault.
- `_bmad/scripts/resolve_current_planning_authority.py` and `_bmad/scripts/verify_evidence_boundary.py` -- preserve V22 and protected-host dispatch while updating authenticated publisher/workflow digests and the closed result contract.
- `_bmad/scripts/tests/test_publish_story_7_1_entry_authority.py`, `_bmad/scripts/tests/test_resolve_current_planning_authority.py`, and `_bmad/scripts/tests/test_verify_evidence_boundary.py` -- add schema truncation/substitution, clean-runner, checkout mismatch, CLI, chronology, recovery, hard-link, race, and rollback faults through production paths.
- `_bmad-output/planning-artifacts/v23-story-7.1-entry-candidate-v1.json` -- regenerate only after the other eight tooling paths are final so its self-excluding manifest binds the amended exact-nine transaction.
- `_bmad-output/planning-artifacts/architecture.md` and `_bmad-output/planning-artifacts/v23-story-7.1-entry-authority-v1.json` -- read-only future-publication targets; do not create or modify them in this cycle.

## Tasks & Acceptance

**Execution:**
- [x] `.github/workflows/planning-authority-preflight.yml`, `_bmad/scripts/publish_story_7_1_entry_authority.py`, and `_bmad/scripts/tests/test_resolve_current_planning_authority.py` -- run protected hosts/result checks with the synchronized candidate-free venv; assert the candidate checkout SHA before any candidate execution; add clean-environment and runnable mismatch faults.
- [x] `_bmad/schemas/v23-story-7.1-entry-authority-v1.schema.json` and `_bmad/scripts/tests/test_publish_story_7_1_entry_authority.py` -- require exact fixed inventory lengths, enumerate the complete ordered 29-path authority source inventory, and replace impossible duplicated blocker/detail equality with a schema-enforceable ledger-reference failure shape; reject every truncation, substitution, and detached blocker.
- [x] `_bmad/scripts/publish_story_7_1_entry_authority.py` and `_bmad/scripts/tests/test_publish_story_7_1_entry_authority.py` -- enforce strict non-boolean integer counts, ancestry-based chronology, phase-specific recovery actions, bounded publication locks, single-link regular files, authority/architecture/authority final reads, and no-replace quarantine recovery; cover all named loop-7 faults plus the real `--write-request` CLI.
- [x] `_bmad/scripts/resolve_current_planning_authority.py`, `_bmad/scripts/verify_evidence_boundary.py`, and their tests -- enforce the revised closed failure shape in both protected hosts, preserve structured results, and repin the exact publisher/workflow bytes.
- [x] `_bmad-output/planning-artifacts/v23-story-7.1-entry-candidate-v1.json` -- regenerate from the final eight non-request tooling blobs; prove the exact-nine mode-`100644` direct-child transaction, self-excluding manifest, raw gitlinks, and deterministic BLOCKED/false output.
- [x] Amend the existing local tooling commit as the single direct child of `e0b098fa1c056385e28ee8ac0efd0c55dfab324f`; keep both spec files outside that commit and do not publish the V23 authority/marker or change sprint, release, push, dependency, submodule, or gitlink state.
- [x] `_bmad/scripts/{resolve_current_planning_authority.py,verify_evidence_boundary.py,publish_story_7_1_entry_authority.py}` and the V23 schema/tests -- authenticate the exact schema digest outside candidate schema semantics and independently require every request and authority decision/result/hold/owner/release/push/execution/story control field; reject loosened-schema over-authorization through both production hosts.
- [x] `_bmad/scripts/publish_story_7_1_entry_authority.py` and publisher tests -- require effective EventStore/Commons successor grants to bind the protected request's raw current gitlinks and the Conversations successor to bind the frozen current source, while retaining exact predecessor identity, owner, repository, requirement scope, and ineffective predecessor state.
- [x] `_bmad/scripts/publish_story_7_1_entry_authority.py` and publisher tests -- bind envelope backup/rebuild/disaster-recovery summaries to the exact canonical runbook procedures; reject every contradictory summary and every action/outcome mutation for all five phases.
- [x] `.github/workflows/planning-authority-preflight.yml` and workflow tests -- explicitly opt the protected job into reviewed fork-head checkout, preserve the candidate-free execution boundary, isolate uv/Python from hostile ambient startup variables, and execute a production-shaped materialize/sync/identity/host/result flow plus the actual checked-in checkout-mismatch gate.
- [x] `_bmad/scripts/publish_story_7_1_entry_authority.py` and publisher tests -- make every operation after exclusive authority creation recoverable, including `fstat` and concurrent hard-link failure; use no-replace quarantine, always report the preserved path after post-rename faults, reject an exact-threshold excess without epsilon, and fault final A1/B/A2 link-count races.
- [x] Preserve the exact-nine mode-`100644` direct-child transaction and regenerate the self-excluding request last after repinning workflow, publisher, schema, resolver, and verifier identities; add real-loader signed-authority integrations for both protected hosts without publishing the authority/marker or changing sprint/release/push/gitlink state.

**Acceptance Criteria:**
- Given unchanged V22 history, `cf82f8008d02b07d48338a545909d97faa302362` remains PASS/false and current HEAD remains FAIL/`CANDIDATE_GRAPH_DRIFT`.
- Given missing approval or gate evidence, the successor returns BLOCKED or FAIL with execution false and a nonempty ledger.
- Given the exact separately approved publication and all gates, evidence-boundary and current-authority checks pass nonvacuously, only Story 7.1 has execution true, and no prohibited path changed.

## Implementation Notes

- Review-loop 1 re-derivation must establish an external executable trust boundary before loading V23 code: the protected-source resolver and evidence verifier must independently pin and verify the exact publisher blob and request publication topology before compilation, and the publisher must never authenticate itself. Historical V22 validation must use an already trusted host callback or a separately pinned blob. Missing/mismatched executable identity is `BLOCKED` with execution false.
- Every evidence read that contributes to a candidate verdict must come from committed candidate/protected-source objects, including active workflows, contexts, schemas, and signature guards. Use an allowlisted Git environment equivalent to the existing V22 `trusted_environment`, clear redirect/config variables, and pin the SSH verification executable and trust material.
- Replace generic keyword/self-declaration acceptance with closed gate-specific proof. The operational-envelope gate must mechanically establish the AD-5 owner, explicit environment/provider/dependency responsibilities, recovery/runbook/scaling dispositions, and dated waiver state rather than token presence. Preservation, performance, and landing-zone dispositions must bind the evaluated commit/tree, canonical gate authority, exact source/evidence hashes, commands/output digests, and a nonempty gate-specific all-PASS ledger; unknown or contradictory data blocks. Mechanically validate the workflow-route inventory instead of hard-coding PASS.
- Close and test result semantics (`PASS=0`, `FAIL=1`, `BLOCKED=2`), malformed/reversed markers, real complete-marker dispatch, real SSH signature accept/reject paths, invalid gate evidence, dirty-worktree isolation, architecture no-clobber behavior, unavailable history/trust, and stable nonempty blockers. A future publication may still enable only Story 7.1 and must keep release, push, and Stories 7.2-7.4 disabled.
- KEEP: preserve the deterministic BLOCKED/false request, prior eight-path tooling core inside the amended direct-child transaction, self-excluding tooling manifest, exact source and raw-mode-`160000` gitlink inventories, immutable V22 PASS/false and protected-merge `CANDIDATE_GRAPH_DRIFT`, two-path signed authority publication, atomic rollback, schema closure, and the existing restoration/anti-vacuity faults.
- Review-loop 2 must move the actual CI trust root outside the evaluated candidate. The protected workflow route must be part of the amended tooling transaction, use the base/protected commit's host bytes for pull-request authority and evidence evaluation, and prove with a hostile coordinated host/publisher fixture that candidate changes cannot redefine the expected digest or verdict. Candidate-owned scripts may remain the proposed successor implementation, but they are never their own authenticator. Use an event model whose workflow definition is protected from the pull-request head, preserve read-only permissions, and keep arbitrary candidate code outside the trust-host process.
- The amended request manifest is the prior eight-path core plus only `.github/workflows/planning-authority-preflight.yml`. Pin and mechanically validate that exact nine-path transaction, the protected workflow definition/route, and every regular-file mode. The prior exact-eight KEEP survives as the unchanged core; the sole expansion is the external caller required to make its trust claim real.
- Replace declarative gate summaries with closed, canonical gate proofs. Each gate must have an exact path and source inventory: preservation enumerates and hash-binds every member of the frozen FR-20/SM-C1 denominator and recomputes complete/no-mismatch status; performance binds the frozen hot-path IDs and raw finite sample sets, enforces the canonical minimum sample count and `<=5%` threshold, recomputes each P95 and regression, and rejects booleans/NaN/infinities; landing-zone evidence binds the exact OQ-1 decision IDs, approving authority identities, decision-object hashes, and zero unresolved decisions. A command assertion must bind exact committed output bytes that the validator parses, or be reproduced by a protected allowlisted command; a declared argv/digest alone is never proof. Require a unique closed ledger row for every canonical source and measurement.
- Make all decision time relationships explicit: evidence evaluation and trusted gate dispositions must be no later than the owner decision, and an ACTIVE waiver must still be unexpired at that decision. Replace arbitrary nonempty operational strings with a closed structured contract whose owner identities, provider/environment IDs, dependency responsibilities, recovery objectives/procedures, runbook reference, scaling limits/strategy, and waiver fields are mechanically validated and cross-bound.
- Both protected hosts must enforce the whole closed result contract after dispatch: schema identity, result/exit mapping, effective and implementation holds, approval/release/push flags, Story 7.1-only execution, blockers, and ledger states. Request routes must reject a complete authority marker and undeclared descendant scope. Use fixed trusted Git/SSH executable identities, timeouts, and hostile ambient-environment tests.
- Complete the missing integration and fault coverage: real protected-workflow hostile-host rejection; complete-marker `resolve_authority`; V23 request and authority through production `verify`; full signed authority resolution with exact publication/owner arguments; contradictory host results; every gate type and operational recovery/scaling fault; all governed dirty-worktree categories through production wiring; redirect/config poisoning; boolean and non-finite measurements; temporal inversions; descendant scope; rollback-error preservation.
- Publication must not overwrite concurrent worktree changes. Create the authority with an exclusive no-replace primitive, append only the exact marker suffix after an identity recheck rather than replacing the architecture prefix, and preserve the originating blocker if rollback itself fails. Keep byte-identical retry behavior and the existing no-dirty-architecture guard.
- Review-loop 3 must make the protected workflow a closed byte identity, not a token-shaped claim. Both protected hosts and the authenticated publisher must pin the exact workflow digest and the exact `TRUSTED_HOST_COMMIT` derivation from the protected event base; a runnable hostile-base/head harness must exercise materialization and prove that changing the head host, publisher, environment binding, or inserting an extra protected-job command is rejected. Preserve the separate read-only `pull_request_target` trust job and unprivileged `pull_request` candidate-validation job, and keep every head-selected executable outside the protected job.
- Landing-zone validation must preserve the canonical OQ-1 record's actual `BLOCKED` predecessor state and must never relabel its blocked decision objects as approved. A future successor disposition must carry explicit new approved decision objects for exactly FR-10 through FR-15, bind each approval/grant authority and its committed object, name the blocked predecessor it supersedes, and hash the new approved objects. Missing, inherited-blocked, invented, duplicate, or unresolved decisions block execution.
- Owner, gate, envelope, and waiver times must be bounded by a trusted observation: reject any decision/evidence instant later than validation time, and on resolution require the owner decision no later than the signed authority publication commit time. An ACTIVE waiver must be live both at the owner decision and verification. Use an injectable clock for deterministic faults. The recovery runbook must use one canonical Story 7.1 production-recovery path, be included in the exact pre-authority evidence transaction, and satisfy a closed identity/procedure inventory rather than merely matching an arbitrary file hash.
- Publication must be safe against symlinks and non-cooperating writers. Resolve writes relative to a verified repository directory descriptor, use no-follow/exclusive primitives, require regular-file and inode identity, verify the exact post-append bytes, and never truncate or unlink when the current inode/content is not the object and byte prefix created by this process. On an ownership race, preserve the other writer's bytes and return a stable BLOCKED result with the original and rollback diagnostics.
- The evidence host must match the current-authority host's complete nonvacuous result validation, including nonempty strings, unique ledger/blocker identities, closed observed fields, matching result states, holds, authorization flags, and Story 7.1-only execution. Add successful signed V23 authority integrations through both production hosts, a real protected materialization harness, over-threshold performance and future-envelope faults, boolean summary rejection, malformed UTF-8 structured failure, symlink/race/rollback ownership faults, and final-byte identity checks.
- KEEP from review loop 3: the direct-child exact-nine mode-`100644` tooling transaction; immutable V22 PASS/false and protected-merge FAIL/false outcomes; deterministic BLOCKED/false request; protected-base dual-event workflow topology and isolated base-owned Python environment; authenticated publisher digest and committed-object reads; fixed Git/SSH identities; closed schemas and current-authority result contract; canonical preservation/performance inventories and recomputation; real owner-signature integration; append-only two-path authority topology; authority-file exclusive create; exact worktree-prefix guard; and all 129 passing restoration, poisoning, temporal, signature, and nonvacuity tests that remain semantically valid.
- Preserve the single publication topology by amending the current tooling commit after re-derivation; do not create a descendant tooling commit. Do not include either implementation spec in that commit, publish V23 authority/marker bytes, or change sprint, release, push, dependency, submodule, or gitlink state.
- Review-loop 4 must isolate the protected workflow before any candidate-rooted interpreter, package installer, inline Python, or Git object read can run. Pin and invoke the Python, Git, and uv bootstrap identities from a candidate-free working directory with a sanitized environment; add hostile `pip`, `json`, Git-redirection, and environment fixtures proving candidate files cannot execute or redirect protected-host materialization. Preserve the base-owned host project and exact workflow-byte binding.
- OQ-1 authorization must bind closed new effective successor grant objects for every grant named by FR-10 through FR-15. The immutable v1 grants remain ineffective predecessors only; each successor grant must explicitly supersede its blocked predecessor, record `effective: true`, a nonfuture effective instant, exact owner/repository/scope, and its committed-object hash. A freshly authored decision object cannot promote an ineffective grant.
- Recompute the canonical preservation denominator digest from the exact ordered IDs instead of copying its declared digest. Cross-validate closed scaling semantics (`FIXED` requires one replica and no active thresholds; `HORIZONTAL` requires coherent limits/thresholds). Close the public authority/result schemas to the same exact gate, ledger, blocker, observation, signature, and authorization inventories enforced at runtime; both protected hosts must pin the exact signer observation and correlate every blocker with its same-state ledger assertion.
- Define the publication success boundary as a final joint descriptor-based validation of both named paths while all publisher-owned descriptors/locks are still held. Recheck the authority inode and bytes after the architecture append, fsync the containing directory after create or unlink, and preserve unrelated bytes on every rollback race. Mutations begun after that final validation are separate external transactions and are outside the publisher's atomicity claim; do not claim protection after return. Add faults for authority replacement during marker append, short writes on every write path, directory-fsync failure, same-inode append races, pathname replacement, and byte-identical retry identity.
- Require the committed evidence-source instant to be no later than every gate/envelope disposition and the owner decision, with all of them no later than validation and the signed authority publication. Add production-path tests for malformed canonical recovery content, the full signed-authority `verify` route, and the owner-facing `--publish-authority` CLI including exact success bytes and partial-write failure behavior.
- Review-loop 5 must give the trusted `pull_request_target` verdict an event-qualified check name that the skipped ordinary `pull_request` job cannot satisfy. Pin the protected Python setup action by immutable digest and its interpreter by exact version; consume only its explicit interpreter path (including uv synchronization and the result checker), disable interpreter downloads, and keep all protected bootstrap commands in the candidate-free temporary directory. Mechanically reject any reintroduction of an ambiguous protected check context or runner-selected Python.
- Make the public request, authority, and result schema accept exactly the runtime language: close request/authority-only fields by result and document kind; encode the fixed ordered gate, ledger, blocker, observation, signature, and authorization inventories; require safe normalized repository paths and strict whole-second UTC `Z` instants; and add schema/runtime parity fixtures for every accepted alternative and rejected near miss. Runtime numeric checks must reject booleans. `FIXED` means exactly one replica and no thresholds; `HORIZONTAL` requires a nonempty replica range and ordered finite thresholds.
- Treat pair publication as a recoverable two-path transaction. No fallible operation may follow the final joint validation that defines the success linearization point. Any failure after marker installation must restore both visible paths; rollback must quarantine before acting, must never truncate or unlink an inode that another writer can still mutate, and must retain/name quarantine evidence whenever exclusive ownership cannot be proved. A same-inode append, pathname swap, post-append authority drift, directory-fsync fault, or open-descriptor mutation must preserve unrelated bytes and return a stable BLOCKED diagnostic. Byte-identical pair retry must be reachable and idempotent.
- Complete the final branch coverage with valid `FIXED`, live `ACTIVE` waiver, nonzero in-threshold performance, and protected-host V23 FAIL fixtures, plus authority short-write and create/remove directory-fsync faults. KEEP the exact direct-child nine-path transaction, immutable V22 outcomes, deterministic BLOCKED request, protected-base host and workflow byte binding, canonical evidence recomputation, real owner-signature integration, append-only authority topology, and all 166 passing tests whose semantics remain valid.
- Review-loop 7 re-derivation must execute resolver, verifier, and result checking with the synchronized protected-project venv interpreter; the setup-Python executable is only uv's pinned base. From the candidate-free directory, assert the venv prefix/base identity, exact Python version, and `jsonschema` import. Candidate validation must compare `HEAD` with the event-selected SHA immediately after checkout and before SDK, package, or repository code execution.
- Close every fixed public-schema inventory with equal minimum and maximum lengths: result states 3; tooling paths 9 and self-excluding manifest 8; request sources 15, gitlinks 10, gates 12, publication paths 2, ledger 12, blockers 5; authority publication paths 2, gates 12, sources 29, gitlinks 10, ledger 12; PASS changed paths 2 and ledger 13; request-BLOCKED ledger 12 and blockers 5. Pin every trailing authority source path, including the envelope, recovery runbook, three successor grants, six FR-10--FR-15 decisions, and the three gate-result paths selected by a closed canonical contract.
- Standard JSON Schema cannot prove equality between arbitrary duplicated blocker and ledger strings. Replace that impossible promise with one canonical failure identity/detail and schema-enforceable blocker-to-ledger index references: generic FAIL/BLOCKED has exactly one non-PASS assertion and one reference to index 0; the fixed request result references exactly its five non-PASS ledger rows. Both protected hosts must validate the same reference model and reject missing, duplicate, out-of-range, PASS-targeting, or contradictory references while preserving stable blocker codes and nonempty ledgers.
- Git ancestry, never author/committer dates, establishes artifact ordering: request publication -> exact evidence-source commit -> signed authority publication. `%aI` and `%cI` are untrusted metadata and must neither authorize nor reject. Embedded whole-second UTC instants are owner-attested business chronology only: successor-grant effective time must be no later than its gate disposition; envelope evaluation and every gate disposition must be no later than the owner decision; the owner decision must be no later than the injected trusted validation observation. An ACTIVE waiver must be live at both owner decision and validation. Evidence instants may predate their containing commit.
- Replace prose-length recovery acceptance with an exact ordered action/outcome inventory for `DETECT`, `CONTAIN`, `RESTORE`, `VERIFY`, and `ESCALATE`: observe health/telemetry and record scope; activate Story 7.1 hold/false and quiesce affected writes; select a verified checkpoint/backup and restore/rebuild; verify stream integrity, projections, and tenant isolation; notify the AD-5 owner, record decision/evidence, and retain the hold pending approval. Reject generic, duplicated, missing, reordered, or cross-phase actions.
- Define the two-path publication linearization point with both descriptors opened first and locked under a bounded acquisition timeout, followed by authority A1, architecture B, and authority A2 descriptor/named-path observations; A1 and A2 must match in bytes, inode, link count, and metadata, so B occurred inside a stable authority interval. No semantic or fallible operation may follow A2. Require `st_nlink == 1` for create, retry, append, quarantine, restore, and final validation. All rollback moves/restores must be no-replace; destination recreation preserves the other writer and quarantine evidence while retaining the originating blocker. Mutations begun after the last verified observation are separate external transactions and are not part of the publisher's atomicity claim.
- After re-derivation, retain the loop-7 patch set: strict integers (never booleans or integral floats) for preservation and landing-zone counts and performance sample counts; production checkout-SHA mismatch coverage; and an end-to-end `--write-request` test asserting exact bytes and digest. Recompute workflow and publisher digests, regenerate the request last, and preserve every earlier KEEP constraint and semantically valid fault.
- Review-loop 8 must treat the V23 schema as data to authenticate, not an executable authorization root. Both protected hosts must pin its exact digest alongside workflow/publisher identity before candidate dispatch; publisher runtime must independently close every request and authority control field even when the schema is maliciously relaxed. Preserve schema validation as an additional layer, not the sole enforcement for result, decision, holds, owner/release/push claims, execution, or Story 7.1-only scope.
- Effective OQ-1 successor grants must not make obsolete snapshots effective. For grants with `gitlinkPath`, the successor `acceptedSourceSnapshot` must equal the raw mode-`160000` object at the frozen protected request; the Conversations consumer successor must bind the frozen protected-main source. Preserve predecessor bytes and hashes, exact grantor/repository identity and requirement scope, but express the refreshed accepted snapshot explicitly in the successor instead of copying the stale predecessor repository object wholesale.
- Cross-bind the operational envelope summaries to the canonical recovery inventory. Backup, rebuild, and disaster-recovery fields must be exact identifiers or exact derived references to the validated DETECT/CONTAIN/RESTORE/VERIFY/ESCALATE actions and outcomes; generic or contradictory prose blocks. KEEP all strict owner, objective, waiver, scaling, digest, and phase-order checks.
- Run protected bootstrap and hosts with isolated-mode Python and an explicit allowlisted environment so hostile `PYTHONPATH`, `PYTHONHOME`, startup modules, loader variables, or unrelated `UV_*` controls cannot execute or redirect imports. The `pull_request_target` checkout must explicitly set the pinned action's fork-head opt-in and prove that checkout is followed only by protected actions/base-owned hosts until the unprivileged job. Test the exact checked-in shell blocks or one shared existing-path entry point rather than copied lookalikes; do not add a tenth tooling path.
- Once exclusive creation succeeds, every later authority-file operation belongs to rollback, including initial `fstat`, link-count validation, write, fsync, and directory durability. On any race, remove the governed visible name only through no-replace quarantine, preserve aliases/concurrent bytes, and include the exact quarantine path in original-plus-rollback diagnostics even when open or directory fsync fails. KEEP bounded locks, single-link checks, A1/B/A2 ordering, idempotent retry, and no fallible semantic work after success linearization.
- Retain the surviving loop-8 patches through re-derivation: a materialized temporary protected-project venv test; execution of the actual checkout identity gate; real-loader signed-authority integrations through both production hosts; all-phase/action/outcome recovery faults; mid-final-read hard-link faults; removal of the performance-threshold epsilon; and quarantine-location diagnostics. KEEP all 202 currently passing tests whose semantics remain valid.

## Spec Change Log

- 2026-09-20 review loop 1: Independent review found that candidate-controlled Python could authenticate itself, applicable evidence checks depended on worktree bytes, and future gate evidence was structurally too weak. The non-frozen implementation notes now require protected-source executable pinning, committed-object-only reads, closed gate-specific provenance, trusted Git/SSH isolation, strict result semantics, and the missing fault coverage. Avoid the known-bad self-validating candidate loader, keyword-only envelope, generic PASS ledger, hard-coded workflow PASS, dirty architecture overwrite, and worktree-schema fallback. KEEP the exact transaction, historical V22 behavior, deterministic blocked request, closed outputs, atomic publication shape, and nonvacuous restoration tests.
- 2026-09-20 review loop 2: Independent review proved that the real workflow still executed candidate-owned host scripts, and that gate records could still self-declare arbitrary inputs, aggregate results, output digests, operational strings, and inconsistent times. The non-frozen code map, tasks, and implementation notes now add the protected workflow as the sole ninth transaction path; require base-owned host execution, canonical replayable gate evidence, full host-result closure, temporal consistency, exact executable isolation, integration/fault coverage, and concurrency-safe append/no-replace publication. Avoid the known-bad candidate-defined trust root, declarative command digest, arbitrary source/ledger inventory, aggregate-only gates, untyped/non-finite measurements, future evidence, expired-at-approval waiver, check-then-replace clobber, and rollback-error masking. KEEP the prior eight-path core and single amended tooling publication, immutable V22 outcomes, deterministic BLOCKED request, committed-object reads, authenticated publisher compilation, schema/result closure, marker parsing, explicit SSH trust material, dirty-worktree isolation, two-path future authority, and no authority/marker/sprint/release/push/gitlink publication.
- 2026-09-20 review loop 3: Independent review found that the protected workflow was recognized by forgeable tokens, the blocked OQ-1 predecessor was projected as approved, future decision times and arbitrary runbook files could pass, the evidence host still admitted vacuous result rows, and symlink/non-cooperating races could modify external bytes or erase another writer's work. The non-frozen implementation notes now require byte-pinned workflow/base derivation with an executable hostile-head harness; explicit successor OQ-1 approval objects that preserve the blocked predecessor; trusted publication/validation time bounds; a canonical validated recovery runbook in the evidence transaction; symmetric host-result closure; no-follow descriptor/inode ownership; race-preserving rollback; and the missing authority, threshold, time, UTF-8, and concurrency integrations. Avoid the known-bad token-only route proof, blocked-to-approved relabeling, future-valid forever decisions, arbitrary runbook hash, pathname rollback, unconditional truncation, and shape-only evidence-host result validation. KEEP the exact direct-child nine-path transaction, immutable V22 behavior, deterministic blocked request, protected-base dual-event topology and environment, authenticated committed-object publisher, fixed trust tools, schema/gate recomputation, signed authority path, append-only publication shape, exclusive authority create, dirty-prefix protection, and every semantically valid existing fault test.
- 2026-09-20 review loop 4: Independent review found candidate-shadowable Python/package bootstrap before protected-host isolation, ineffective OQ-1 grants reused as approval, copied preservation digests, schemas and hosts looser than the claimed closed result, incomplete joint publication identity/durability, evidence times that may predate their commit, and three missing production-path tests. The non-frozen notes now require candidate-free pinned workflow bootstrap, effective successor grants, recomputed denominators, cross-field scaling and exact schema/host closure, a realizable final joint descriptor boundary with directory durability, commit-bounded times, and the missing runbook/verifier/CLI integrations. Avoid the known-bad candidate-cwd `python -m pip`/inline imports, hash-only blocked grants, copied digests, shape-only schemas, one-path final checks, unbounded backdating, and helper-only coverage. KEEP the exact direct-child nine-path transaction, immutable V22 outcomes, deterministic BLOCKED request, authenticated committed-object publisher, canonical raw evidence recomputation, real SSH verification, append-only two-path topology, no-clobber authority creation, exact architecture prefix, and every semantically valid prior restoration and fault test.
- 2026-09-20 review loop 5: Independent review found that an ordinary pull request could emit a skipped check under the trusted job's context, the protected host still selected mutable runner Python, public schemas remained looser than runtime, scaling/numeric alternatives were incomplete, byte-identical retry was unreachable, and failures after marker installation could leave a partial pair or destroy concurrent bytes during rollback. The non-frozen notes now require event-qualified trusted check identity, digest-pinned exact Python consumption, exact schema/runtime language parity, strict scaling and boolean rejection, fully reachable retry semantics, and quarantine-based rollback of both visible paths with no fallible work after the success linearization point. Avoid the known-bad shared skipped context, runner-selected interpreter, shape-only conditional schema, one-sided rollback, in-place truncate/unlink, and pre-retry source-only guard. KEEP the exact direct-child nine-path transaction, immutable V22 results, deterministic BLOCKED request, protected-base byte bindings, canonical evidence and real signature checks, two-path append-only authority model, and all 166 semantically valid passing tests.
- 2026-09-20 review loop 7 corrective cycle: Independent review found the protected host bypassed its synchronized dependencies, fixed schema arrays remained truncatable, fourteen authority bindings were generic, FAIL correlation was not schema-expressible, pair validation lacked a bracketing reread and single-link/no-replace guarantees, committer time was treated as trusted chronology, and recovery phases accepted generic prose. The re-derived non-frozen plan now requires the protected venv, exact inventories and canonical paths, index-referenced failures, A1/B/A2 final validation, bounded locks, hard-link rejection, no-replace recovery, ancestry plus owner-attested times, and phase-specific recovery actions. After re-derivation it retains checkout identity, strict numeric, and CLI patch coverage. Avoid the known-bad base-interpreter launch, prefix-only schemas, duplicated free-text correlation, one-pass pair read, hard-linked writes, replacing rollback, `%cI` authority, and token-shaped recovery. KEEP the frozen intent, every accumulated KEEP, exact-nine direct-child transaction, immutable V22 results, deterministic BLOCKED request, protected-base trust boundary, committed-object evidence, two-path future authority, and all semantically valid tests.
- 2026-09-20 review loop 8 corrective cycle: Independent review found that candidate-owned schema relaxation could bypass request/authority control fields, successor grants copied stale accepted snapshots, recovery summaries could contradict the canonical runbook, protected Python still inherited hostile startup variables, fork PRs could not reach the protected hosts, and post-create link/metadata faults escaped cleanup. The non-frozen plan now requires externally pinned schema bytes plus runtime control closure, current-snapshot effective grants, exact runbook-summary cross-binding, isolated bootstrap/host environments with reviewed fork checkout, and quarantine coverage from the first post-create operation. It also retains the verified test gaps for the production-shaped venv/workflow path, actual checkout gate, real signed loaders, every recovery phase, final hard-link races, exact performance threshold, and quarantine diagnostics. Avoid the known-bad schema-as-authority root, copied stale repository object, contradictory recovery prose, ambient Python startup, fork checkout dead-end, and visible empty authority residue. KEEP the exact-nine direct child, immutable V22 PASS/false and protected-merge FAIL/false, deterministic BLOCKED request, all exact schema inventories, current committed-object evidence, synchronized protected venv, strict numerics, bounded A1/B/A2 single-link publication, no-replace rollback, no V23 authority/marker publication, and all semantically valid passing tests.

## Review Triage Log

| ID | Verdict | Evidence | Route |
| --- | --- | --- | --- |
| VG-1 | high | All authority-success tests replace `verify_publication_signature`; no test executes the author, SSH status, principal, fingerprint, revoked-key, or unavailable-anchor checks, so this authentication boundary can regress while the suite stays green. | patch |
| VG-2 | medium | The complete-marker test replaces both `v23_marker_selected` and `load_v23_publisher`; no test drives a real complete committed marker through the production selector and loader. | patch |
| VG-3 | high | Existing fixtures provide only valid envelopes and gate evidence; no negative test covers missing AD-5 topics, wrong gate IDs, non-PASS or empty ledgers, or unexpected evidence properties. | patch |
| VG-O1 | high | `validate_v23_scope` and the current resolver execute the candidate's publisher before an independently trusted reader establishes its digest and authority, so a candidate can self-validate a fabricated PASS. | bad_spec |
| VG-O2 | medium | `v23_marker_selected` calls `architecture.index(V23_END, begin)` outside its `ValueError` guard; one END before one BEGIN raises instead of producing structured `BLOCKED`. | patch |
| EC-1 | medium | The sole-END-before-BEGIN marker ordering reaches the unguarded `index` call and escapes the resolver's closed result model. | patch |
| EC-2 | high | A complete marker dispatches to candidate-controlled publisher bytes before authentication; that module can return PASS/true without the trusted signature path. | bad_spec |
| EC-3 | high | The dynamic module call has no trusted interface boundary; missing APIs or `SystemExit` escape the narrow `ResolutionError` handler. This is another consequence of executing untrusted candidate code. | bad_spec |
| EC-4 | high | `verify_evidence_boundary.py` compiles the evaluated candidate's verifier and trusts its `validate_request`/`resolve_published_authority` answers, allowing the evidence gate itself to be redefined. | bad_spec |
| EC-5 | high | `validate_v22_history` compiles the request publication's resolver before any protected-source digest authenticates those executable bytes. | bad_spec |
| EC-6 | low | `getattr(module, "resolve_v22_authority", getattr(module, "resolve_authority"))` eagerly evaluates the fallback and would reject a future resolver exposing only the preferred API; the current pinned resolver exposes both, so present harm is limited. | patch |
| EC-7 | medium | The result schema constrains holds for FAIL/BLOCKED but does not bind FAIL to exit 1 or BLOCKED to exit 2, allowing schema-valid contradictions to the declared result semantics. | patch |
| EC-8 | high | AD-5 validation is only case-insensitive token presence; a document denying backup, telemetry, recovery, ownership, or waiver validity can still name every token and pass. | bad_spec |
| EC-9 | high | Gate evidence is trusted solely from four self-declared fields and any nonempty all-PASS ledger; it has no gate-specific measurements, command/candidate provenance, freshness, or trusted disposition identity. | bad_spec |
| EC-10 | high | The V23 Git environment inherits user/global configuration and redirect variables, and signature verification does not pin the SSH program; ambient configuration can redirect object reads or the signature executable. | patch |
| BH-1 | high | Both current-authority and evidence-boundary routes execute candidate Python before a protected-source trust root validates it. | bad_spec |
| BH-2 | high | Candidate-defined module APIs and arbitrary termination can escape structured fail-closed handling; the reachable hostile-module case is caused by the missing executable trust boundary. | bad_spec |
| BH-3 | medium | Reversed marker ordering raises at the unguarded end-marker search instead of returning `V23_MARKER_INCOMPLETE`. | patch |
| BH-4 | high | A one-row `FIXTURE` PASS ledger satisfies preservation, performance, or landing-zone evidence without proving the named gate. | bad_spec |
| BH-5 | high | Keyword-only envelope validation does not establish the ownership, procedures, limits, dispositions, or dated waiver semantics required by AD-5. | bad_spec |
| BH-6 | medium | `CURRENT-WORKFLOW-ROUTE` is hard-coded PASS without parsing the bound inventory or executing its conformance proof, so the request does not mechanically establish this entry gate. | bad_spec |
| BH-7 | false | The governing runbook requires exact equality of the final candidate boundary, which the endpoint diff enforces; reverted intermediate tree states do not alter the evaluated committed state and no requirement mandates a direct evidence-source child. | reject |
| BH-8 | medium | `publish_authority_pair` snapshots a dirty architecture file but compares only against that dirty snapshot before overwriting it with the committed prefix, silently discarding pre-existing user edits. | patch |
| BH-9 | high | V23's inherited Git environment is weaker than the existing resolver's allowlisted `trusted_environment` and leaves committed-object and SSH verification sensitive to ambient redirects/configuration. | patch |
| BH-10 | medium | Failure-result validation reads the worktree schema, so unrelated dirty or absent bytes can replace the stable original blocker with `V23_RESULT_SCHEMA_UNAVAILABLE`. | patch |
| BH-11 | high | Active-route, context, context-workflow, and signature-guard checks read the worktree even though the command claims to validate a committed candidate, making the verdict depend on unrelated checkout bytes. | bad_spec |
| BH-12 | medium | The schema accepts FAIL/BLOCKED result documents with exit code 0 and does not require their ledger to contain a matching non-PASS assertion. | patch |
| BH-13 | high | Every passing authority test injects a successful signature lambda, leaving the real pinned-key boundary without mutation coverage. | patch |
| BH-14 | maybe-false | In “nonzero, skip-free PASS,” nonzero can describe executed fault coverage rather than pytest's exit code; the 89-test run exited 0. Any clarification would edit this build's spec, so it is not an implementation finding. | reject |
| BH2-1 | high | The protected workflow checks out the candidate and directly executes both candidate-owned host scripts, so a coordinated host, digest, and publisher change can self-authenticate before any protected-source executable establishes trust. | bad_spec |
| BH2-2 | medium | The current-authority host preserves ambient `PATH`, invokes bare `git`, and has no timeout; a wrapper or hung executable can forge or stall every topology check despite the required allowlisted environment. | patch |
| BH2-3 | medium | Production signature verification discovers `ssh-keygen` through a path beginning with `/usr/local/bin`; unlike the test's explicit `/usr/bin/ssh-keygen`, this does not pin the executable required by the implementation notes. | patch |
| BH2-4 | high | Gate records merely declare an argv, zero exit, and syntactic output digest; no committed output bytes or trusted replay binds that digest, so fabricated command provenance can authorize a gate. | bad_spec |
| BH2-5 | high | Each gate accepts any nonempty candidate source list, and the passing fixture binds only the V23 request rather than a canonical gate input inventory. | bad_spec |
| BH2-6 | high | Preservation proves only three aggregate counts and never identifies the FR-20/SM-C1 denominator members, allowing an invented one-item denominator to pass. | bad_spec |
| BH2-7 | high | Performance validation neither recomputes regression from the two P95 values nor rejects non-finite numbers or pins the governing threshold/sample floor, so contradictory evidence can pass. | bad_spec |
| BH2-8 | high | Landing-zone validation accepts aggregate counts and arbitrary strings without decision identities, approving authorities, or decision-object hashes, so an invented approval can satisfy OQ-1. | bad_spec |
| BH2-9 | high | carried BH-4: a single gate-prefixed PASS row remains a complete accepted ledger; uniqueness and a closed assertion inventory are not required. | bad_spec |
| BH2-10 | high | Parsed envelope, gate-disposition, waiver, and owner-decision timestamps are not ordered; evidence decided after approval or a waiver expired before approval can support PASS. | bad_spec |
| BH2-11 | high | The operational envelope treats one-character non-placeholder strings as providers, owners, dependencies, recovery procedures, runbooks, and scaling dispositions, which does not establish the actionable AD-5 contract. | bad_spec |
| BH2-12 | high | Both hosts accept result objects without enforcing holds, owner/release/push flags, Story 7.2-7.4 flags, schema identity, or all-PASS authority ledgers, so contradictory authorizations can cross the host boundary. | bad_spec |
| BH2-13 | false | The frozen contract permits drift to be either FAIL or BLOCKED/false, and the stricter BLOCKED rule names executable identity rather than request descendant drift; the default FAIL state does not violate the specified semantics. | reject |
| BH2-14 | medium | The real complete-marker test stops at selector and loader calls, so it does not cover `resolve_authority`, publisher execution, host-result validation, or final exit semantics. | patch |
| BH2-15 | high | The full authority-success test injects a successful signature lambda while the real SSH test exercises only the primitive, leaving the decisive full-path signature wiring unproved. | patch |
| BH2-16 | medium | `atomic_write` checks the destination and later calls replacing `os.replace`; a concurrent edit in that window is overwritten despite the no-clobber/dirty-architecture guarantee. | bad_spec |
| VG2-1 | high | Pre-verified: the workflow invokes candidate-owned hosts and no hostile coordinated host/publisher test or protected-source launcher exists, so the external trust boundary is not adopted at the actual entry point. | bad_spec |
| VG2-2 | high | Pre-verified: full authority tests stub signature success and do not prove that resolution invokes the verifier with the exact publication and owner. | patch |
| VG2-3 | high | Pre-verified: negative coverage exercises only preservation evidence plus two envelope faults; performance, landing-zone, recovery, runbook, and scaling rejection branches can regress unnoticed. | patch |
| VG2-4 | medium | Pre-verified: dirty-worktree isolation is tested only for the route helper, while production `verify` wiring for routes, context, context workflows, and the signature guard is stubbed or unexercised. | patch |
| VG2-5 | high | Pre-verified: tests authenticate the V23 verifier loader but never drive `verify` through either V23 request or authority routing, so route removal could silently fall back to an older authority. | patch |
| VG2-6 | high | Pre-verified: neither host has contradictory returned-document tests for result/exit, blockers, ledgers, or execution permission, leaving host-owned result enforcement regression-prone. | patch |
| VG2-7 | medium | Pre-verified: the new publisher and evidence-host Git environments have no hostile redirect/configuration tests, so ambient-isolation regressions would remain green. | patch |
| VG2-O1 | medium | Python booleans compare equal to integers; preservation `evaluatedCount`/`mismatchCount` and landing-zone `decisionsApproved`/`unresolvedCount` can therefore satisfy numeric checks without numeric values. | patch |
| EC2-1 | medium | Preservation validates the denominator type only; boolean evaluated and mismatch counts compare equal to the expected integers and pass. | patch |
| EC2-2 | high | The recorded regression percentage is not recomputed from baseline and candidate P95 values, so mutually contradictory performance measurements can pass. | bad_spec |
| EC2-3 | high | Python's JSON loader accepts non-finite floats and the performance validator has no finiteness guard, allowing meaningless infinite measurements through selected comparisons. | patch |
| EC2-4 | medium | Landing-zone validation checks only the required-count type; boolean approved and unresolved counts compare equal to integers and pass. | patch |
| EC2-5 | high | An ACTIVE waiver is compared only with envelope evaluation time, not owner-decision time, so a waiver already expired at approval can support execution. | bad_spec |
| EC2-6 | high | Gate disposition timestamps are parsed but never required to precede the owner decision, so future-dated evidence can be presented as already approved. | bad_spec |
| EC2-7 | medium | A request descendant can add a complete V23 marker while the evidence verifier still selects and passes the request route, producing inconsistent route diagnostics even though current-authority correctly blocks. | patch |
| EC2-8 | medium | The architecture and authority writes retain check-then-replace windows, so concurrent changes can be clobbered despite the promised no-clobber behavior. | bad_spec |
| EC2-9 | medium | Rollback catches only `OSError`, but `atomic_write` reports failures as `EntryAuthorityError`; a restoration failure can replace the original publication blocker and leave incomplete recovery. | patch |
| EC2-10 | medium | The V23 request route authenticates the original tooling publication but imposes no closed descendant-scope rule, so unrelated descendant paths can receive a PASS evidence-boundary result while authority remains blocked. | patch |
| EC2-11 | high | Current-authority host validation omits hold, authorization, story, ledger-state, and schema invariants, allowing contradictory publisher results through after dispatch. | bad_spec |
| BH3-1 | false | The ordinary `pull_request` candidate job and the base-owned `pull_request_target` authority job are deliberately distinct named checks; a green candidate test does not make a failing protected-authority check green, and the specification does not require an unsafe cross-event artifact handshake. | reject |
| BH3-2 | false | A manual run is explicitly user-invoked from its selected workflow ref and is not the protected pull-request authority check; no merge or authority outcome was shown to consume a malicious-branch manual result. | reject |
| BH3-3 | high | The successor route accepts token presence and coarse ordering, so extra executable steps or decoy strings can survive while the validator claims the protected job is closed. This violates the required mechanically pinned protected route. | bad_spec |
| BH3-4 | false | The V22 inventory remains an immutable historical source, while the successor branch separately validates and manifest-binds the committed V23 workflow bytes; it does not execute or treat the V22 command as the V23 command. | reject |
| BH3-5 | high | Landing-zone validation hashes the currently `BLOCKED` decision rows but unconditionally projects them as `APPROVED`, so unchanged blocked OQ-1 evidence can support execution. | bad_spec |
| BH3-6 | false | The preservation output is recomputed against every canonical denominator and category row, committed in the exact evidence transaction, and hash-bound by the trusted owner's signed authority; the claimed arbitrary one-row proof no longer passes. | reject |
| BH3-7 | false | Performance raw samples, P95 values, threshold, source bytes, output bytes, and ledger rows are recomputed and ultimately bound by the trusted owner's signed authority. Requiring an independently replayed benchmark is beyond the approved embedded-output proof model. | reject |
| BH3-8 | high | Owner and evidence times are ordered only against a self-declared owner instant; no trusted present/publication boundary rejects a future owner decision and future waiver, so impossible future evidence can remain valid. | bad_spec |
| BH3-9 | medium | The envelope hash-binds any repository-relative file as a runbook without checking a canonical runbook identity or recovery content, so an unrelated file can satisfy the recovery reference. | bad_spec |
| BH3-10 | high | Architecture publication follows a worktree symlink and opens it without `O_NOFOLLOW` or a regular-file/inode check, allowing the marker append to modify an external file. | bad_spec |
| BH3-11 | false | The exact authority commit intentionally unlocks local Story 7.1 work while release and push remain false; dirty implementation can be built and validated on that HEAD, and committing/pushing later state deliberately requires a successor authority. | reject |
| BH3-12 | high | The evidence host checks row keys and states but not nonempty strings or unique identities, so vacuous assertion/blocker rows cross a host that the specification requires to enforce the whole closed result contract. | bad_spec |
| BH3-13 | medium | The unrelated Story 7.1 spec was already user-modified before this build and records `in-progress` under an active hold; it is a real lifecycle inconsistency but was not caused by this story. | defer |
| VG3-1 | high | Pre-verified: no executable harness proves `TRUSTED_HOST_COMMIT` is sourced from the event base; changing the environment binding to the candidate preserves all current token/order assertions. | patch |
| VG3-2 | high | Pre-verified: successful signed authority tests call the publisher directly, while no V23 PASS result traverses `resolve_authority`; production dispatch could reject every valid authority without failing tests. | patch |
| VG3-3 | high | Pre-verified: production `verify` has no complete V23 authority-route integration test, so its authority dispatch can fail or misroute while request-route tests remain green. | patch |
| VG3-4 | high | Pre-verified: no finite, internally consistent raw sample set above the five-percent limit tests the actual threshold branch; removing that comparison leaves the suite green. | patch |
| VG3-5 | high | Pre-verified: no authority-rendering test places the operational envelope after the owner decision, so the envelope temporal guard can regress unnoticed. | patch |
| VG3-O1 | high | A non-cooperating writer can append after the locked identity check; publication then returns successfully with unexpected architecture bytes because advisory locking alone does not prove the final identity. | bad_spec |
| EC3-1 | high | The validator does not bind the `TRUSTED_HOST_COMMIT` environment assignment, so redirecting host extraction to the candidate retains every currently required token. | bad_spec |
| EC3-2 | medium | `False` compares equal to recomputed `0.0` in the measurements dictionary, so a boolean `observedRegressionPercent` passes as a numeric zero. | patch |
| EC3-3 | high | Append rollback unconditionally truncates to the old prefix; a concurrent writer's bytes introduced after the identity check are erased along with this process's partial marker. | bad_spec |
| EC3-4 | high | Authority rollback unlinks by pathname without proving inode/content identity, so it can delete another writer's replacement after the created authority is swapped. | bad_spec |
| EC3-5 | medium | Invalid UTF-8 in a committed active route raises `UnicodeDecodeError`, which `main` does not catch, so the verifier exits without its structured blocker document. | patch |
| EC3-6 | high | The evidence host accepts empty assertion/blocker text and duplicate IDs, contradicting the required nonvacuous closed result contract. | bad_spec |
| EC3-7 | false | The acceptance row's “current HEAD” is explicitly the frozen `dcba5d4...` merge, which still returns `CANDIDATE_GRAPH_DRIFT`; the new technical request's `CANDIDATE_PARENT_DRIFT` is a different candidate. | reject |
| BH4-1 | false | The 11-path review diff intentionally includes two uncommitted spec files, while the committed candidate transaction at `c61393d...` contains exactly the nine manifest paths and the evidence gate reports the specs separately as worktree paths. | reject |
| BH4-2 | medium | carried BH3-13: the unrelated Story 7.1 spec still records `in-progress` while execution is held false; this predates and is not caused by the successor-authority story. | defer |
| BH4-3 | false | A workflow cannot protect the PR that first introduces it from the base branch, but this transaction publishes only a fail-closed request; the separately authorized authority publication occurs only after the protected workflow is on the base, so no execution permission crosses the bootstrap gap. | reject |
| BH4-4 | high | The protected job checks out candidate bytes before running candidate-cwd `python3 -m pip`, inline Python, and bare Git, so candidate module shadowing and ambient object redirection can execute before the base-owned hosts establish trust. | bad_spec |
| BH4-5 | high | `validate_landing_zone_successor` binds the old grant blobs without validating that all three explicitly say `effective: false` and require effective successor grants; new decision JSON can therefore promote blocked grants. | bad_spec |
| BH4-6 | high | Preservation validation copies `testIdsSha256` into its expected measurements but never recomputes it from the ordered `testIds`, contradicting the required independent denominator proof. | patch |
| BH4-7 | false | The category mappings intentionally cover governed preservation subsets, not all 473 denominator tests, and the immutable manifest blob is exact-transaction bound; requiring their union to equal the complete denominator would reject the canonical 192-ID mapping. | reject |
| BH4-8 | medium | Scaling validation accepts `FIXED` with unequal replica bounds and active thresholds, an internally contradictory closed disposition that can authorize an ambiguous production envelope. | patch |
| BH4-9 | high | The public AUTHORITY schema allows repeated arbitrary PASS gates and ledgers, and the result schema leaves observations open even though the artifact claims schema-level closure; schema-only consumers can accept records the runtime rejects. | bad_spec |
| BH4-10 | medium | Both hosts accept any nonempty `G` signer observation instead of independently matching the pinned principal, fingerprint, and author identity, allowing a contradictory trusted-publisher result through the host contract. | patch |
| BH4-11 | medium | Both hosts require only some same-state ledger row and some blocker, without requiring each blocker code to match its non-PASS ledger assertion, so contradictory failure causes pass host validation. | patch |
| BH4-12 | high | `publish_authority_pair` never revalidates the authority pathname/inode/content after the architecture append, so replacement during the append can yield a reported-success mismatched pair. | bad_spec |
| BH4-13 | high | carried VG3-O1: a non-cooperating architecture writer can still mutate after the locked final read and before return, so the implementation overclaims final-byte protection. | bad_spec |
| BH4-14 | high | carried EC3-3 and EC3-4: rollback still has check-then-act windows in which same-inode appends can be truncated or a replaced authority pathname can be unlinked. | bad_spec |
| BH4-15 | medium | Authority creation and removal fsync file contents but not the containing directory, so a crash can lose a reported create or resurrect a reported rollback. | patch |
| BH4-16 | high | The owner and disposition instants are not lower-bounded by the evidence-source commit, so an authority can claim that approval occurred before the committed evidence it approves existed. | bad_spec |
| BH4-17 | medium | The in-review spec leaves every execution task unchecked and records only expected verification commands, but the only direct fix is to edit this build's spec, which review policy rejects. | reject |
| VG4-1 | high | Pre-verified: all production authority paths call `validate_recovery_runbook`, but no test supplies canonical-path, digest-matched malformed committed runbook content; deleting that call leaves the current suite green. | patch |
| VG4-2 | high | carried VG3-3: production `verify` still has no full signed V23 authority-route integration, so authority dispatch can regress while helper-level signed-authority tests remain green. | patch |
| VG4-3 | medium | Pre-verified: no test invokes the owner-facing `--publish-authority` CLI, so it can report success without writing the authority/marker pair or miswire evidence arguments. | patch |
| EC4-1 | high | Candidate-rooted Python commands execute before and between the protected verifiers; without a candidate-free safe path and fixed bootstrap identities, a candidate `pip` or standard-library shadow can run inside the trust job. | bad_spec |
| EC4-2 | high | carried EC3-4: `remove_owned_file` verifies an opened inode and then unlinks the pathname, leaving a replacement window that can delete another writer's file. | bad_spec |
| EC4-3 | high | `publish_authority_pair` performs no final authority identity check after marker publication, duplicating the mismatched-pair outcome in BH4-12. | bad_spec |
| EC4-4 | false | The short-write defect is confined to the `no_clobber=False` branch, but production has no caller of that branch; both request and authority publication use the looped, verified `no_clobber=True` path. | reject |
| EC4-5 | high | The claimed protected-base-only execution is defeated by candidate-cwd bootstrap and inline Python before the protected hosts run, the same trust-root defect as BH4-4 and EC4-1. | bad_spec |
| BH5-1 | false | Marker-free V23 request descendants deliberately remain on the last-marker V22 route; the request is authenticated by the evidence verifier but cannot select the successor authority resolver until a complete authority marker exists. | reject |
| BH5-2 | high | The ordinary `pull_request` event creates a skipped `protected-planning-authority` job with the same required-check context as the trusted `pull_request_target` event, so branch protection may accept the skipped context instead of a real protected verdict. | bad_spec |
| BH5-3 | high | If final authority revalidation or directory fsync fails after the marker append, rollback removes only the authority and leaves a partial published marker. | bad_spec |
| BH5-4 | medium | carried BH4-8: `FIXED` scaling still accepts unequal replica bounds and active thresholds despite the closed one-replica/no-threshold contract. | patch |
| BH5-5 | medium | `HORIZONTAL` scaling accepts equal minimum and maximum replica counts, leaving no actual horizontal range despite the required coherent limits. | patch |
| BH5-6 | false | Finite ordered scaling thresholds can legitimately be negative for signed metrics; the closed contract does not constrain the metric domain to nonnegative values. | reject |
| BH5-7 | medium | carried EC2-1: preservation `mismatchCount: false` still compares equal to numeric zero and passes. | patch |
| BH5-8 | medium | carried EC2-4: landing-zone `unresolvedCount: false` still compares equal to numeric zero and passes. | patch |
| BH5-9 | false | Candidate timing samples are required to be finite and nonnegative; zero is a valid measurement and the strictly positive baseline P95 keeps the regression calculation defined. | reject |
| BH5-10 | high | carried BH4-9: the public authority schema still permits PASS-only fields on non-PASS records and incomplete conditional closure. | bad_spec |
| BH5-11 | high | carried BH4-9: schema `uniqueItems` does not establish unique assertion IDs/blocker codes or blocker-to-ledger correlation. | bad_spec |
| BH5-12 | high | carried BH4-9: request and authority core objects still expose open inventories that schema-only consumers can accept although runtime rejects them. | bad_spec |
| BH5-13 | medium | Public schema path and timestamp forms are looser than runtime: dot components/control characters and offset/fractional RFC3339 times can pass schema but fail the exact runtime contract. | bad_spec |
| BH5-14 | high | The protected workflow pins uv but still selects mutable runner-provided `python3` and `/usr/bin/python3` identities, violating the promised pinned Python bootstrap identity. | bad_spec |
| VG5-1 | medium | Pre-verified: no positive authority fixture covers valid `FIXED` scaling or the live `ACTIVE` waiver alternative, so those accepted contract branches can regress undetected. | patch |
| VG5-2 | medium | Pre-verified: performance tests cover zero and over-limit regression but not a valid nonzero regression at or below five percent. | patch |
| VG5-3 | medium | Pre-verified: protected-host tests never accept a valid V23 `FAIL` document, leaving the non-PASS result contract only negatively exercised. | patch |
| VG5-4 | medium | Pre-verified: authority publication does not inject a short write into the authority-file write path, leaving its write-all guarantee unproved. | patch |
| VG5-5 | medium | Pre-verified: no fault test covers directory-fsync failure after authority creation or rollback removal. | patch |
| VG5-O1 | medium | Byte-identical pair retry is unreachable because the architecture precheck requires the original prefix even after an identical marker was already published. | patch |
| EC5-1 | high | Final authority revalidation or directory-fsync failure after marker append removes only the authority, leaving the marker and violating pair rollback. | bad_spec |
| EC5-2 | high | carried EC3-3 and BH4-14: a same-inode append after rollback validation but before truncate can still be erased by rollback. | bad_spec |
| EC5-3 | high | A writer holding the created authority descriptor can mutate its quarantined inode after validation but before unlink, so rollback removes the name without preserving those unrelated bytes. | bad_spec |
| EC5-4 | high | carried VG3-O1 and BH4-13: architecture mutation after the final append check but before success return can still yield a reported-success mismatched pair. | bad_spec |
| EC5-5 | false | Blockers intentionally bind a failed ledger assertion by code/detail; they need not equal the aggregate `BLOCKED` result when the ledger contains an underlying `FAIL` cause. | reject |
| EC5-6 | false | The evidence host uses the same intentional blocker-to-failed-assertion correlation; forcing every blocker row to the aggregate result would reject valid mixed-cause BLOCKED documents. | reject |
| BH6-1 | high | The protected workflow syncs dependencies into the host project environment but explicitly executes the setup-Python base interpreter, so authenticated V23 loading can fail to import `jsonschema` on a clean runner. | bad_spec |
| BH6-2 | false | The protected job runs on a fresh GitHub-hosted runner, strips ambient Git variables, and checkout does not fetch repository replacement refs; no candidate-reachable replacement-ref source was demonstrated. | reject |
| BH6-3 | high | carried BH4-9 and BH5-10 through BH5-12: request arrays use `prefixItems`/`items: false` without exact minimum lengths, so schema-only consumers still accept truncated inventories. | bad_spec |
| BH6-4 | high | carried BH4-9 and BH5-10 through BH5-12: authority gates, ledger, and gitlinks likewise remain truncatable below the runtime-required inventory. | bad_spec |
| BH6-5 | high | carried BH4-9 and BH5-10 through BH5-12: PASS and request-BLOCKED result branches do not require their complete runtime ledger/blocker lengths. | bad_spec |
| BH6-6 | high | carried BH4-9 and BH5-10 through BH5-12: the FAIL schema branch does not correlate its one blocker code/detail with its one ledger assertion as both protected hosts do. | bad_spec |
| BH6-7 | medium | carried EC2-1 and BH5-7: preservation measurement dictionary equality still accepts `mismatchCount: false` as numeric zero. | patch |
| BH6-8 | medium | carried EC2-4 and BH5-8: landing-zone measurement equality still accepts boolean zero-equivalents and integral floats for integer counts. | patch |
| BH6-9 | medium | Performance summary validation accepts `sampleCount: 30.0` because it treats all summaries as generic finite numbers and equality equates the float with the recomputed integer count. | patch |
| BH6-10 | high | carried VG3-O1 and BH4-13: final pair validation reads authority before architecture and does not reread authority, so a non-cooperating writer can mutate the first inode during the second read before success returns. | bad_spec |
| BH6-11 | high | Publication accepts multiply linked regular files, so an identical hard-linked architecture path can append the marker into an external alias. | bad_spec |
| BH6-12 | high | Rollback restores a quarantined pathname with replacing `rename`; a writer that recreates the destination during the recovery window can have its new file overwritten. | bad_spec |
| BH6-13 | high | Git committer time is caller-controlled even on a later signed descendant, so treating `%cI` as an independently trusted evidence/publication clock does not establish the claimed chronology. | bad_spec |
| BH6-14 | medium | carried BH4-17: execution tasks remain unchecked inside the frozen block, but review policy rejects editing this build's frozen spec to fix that bookkeeping. | reject |
| BH6-15 | medium | carried BH3-13 and BH4-2: the unrelated Story 7.1 spec remains `in-progress` under an active hold; this predates and is not caused by the successor implementation. | defer |
| BH6-16 | false | The governed `.md` paths are fixed predecessor contract names while the successor deliberately defines their exact machine-readable JSON bytes; file extensions do not change what the validator accepts. | reject |
| BH6-17 | high | carried BH2-11: exact procedure IDs still accept the same generic two-word step text for every recovery phase, so the operational contract remains token-shaped rather than actionable. | bad_spec |
| VG6-1 | high | Pre-verified: candidate validation has no post-checkout assertion that `HEAD` equals the event-selected candidate, so the required suite can run against the wrong revision without a test failing. | patch |
| VG6-2 | medium | Pre-verified: no test invokes `--write-request`, allowing the operator CLI to report success without creating the exact request artifact. | patch |
| EC6-1 | false | carried BH3-2: a manual branch run is user-selected, uses an event-qualified protected check name, and is not the protected pull-request authority context; no merge or authority consumer of that run was shown. | reject |
| EC6-2 | high | Both pull-request event runs still emit the same `candidate-validation` context, so the skipped `pull_request_target` instance can mask or ambiguously satisfy the real unprivileged candidate check. | bad_spec |
| EC6-3 | high | carried BH4-9 and BH5-10 through BH5-12: absent exact `minItems` constraints allow schema-valid truncated request, authority, and result inventories. | bad_spec |
| EC6-4 | high | Publication lock acquisition uses an unbounded blocking `flock`; a stalled holder can hang the owner CLI indefinitely instead of producing a stable fail-closed result. | bad_spec |
| EC6-5 | false | carried EC3-7: the frozen acceptance row's “current HEAD” names the explicit `dcba5d4...` protected merge, which still returns `CANDIDATE_GRAPH_DRIFT`; the successor request commit is a different candidate. | reject |
| BH7-1 | high | carried BH6-1: the protected workflow still syncs dependencies into the host project environment and executes the setup-Python base interpreter, so authenticated V23 loading can fail to import `jsonschema` on a clean runner. | bad_spec |
| BH7-2 | high | carried VG6-1: candidate validation still has no post-checkout assertion that `HEAD` equals the event-selected candidate, so the required suite can run against the wrong revision without failing. | patch |
| BH7-3 | high | carried BH6-3 and EC6-3: `resultSemantics.states` still uses `prefixItems` with `items: false` and no exact minimum length, so schema-only consumers accept a truncated state inventory. | bad_spec |
| BH7-4 | high | carried BH6-3 and EC6-3: tooling `exactChangedPaths` and `manifest` still omit exact minimum lengths, so empty or truncated transaction inventories remain schema-valid. | bad_spec |
| BH7-5 | high | carried BH6-3 and EC6-3: request `sourceBindings`, `rootGitlinks`, and `entryGates` still omit exact minimum lengths, so schema-only consumers accept incomplete inventories. | bad_spec |
| BH7-6 | high | carried BH6-3, BH6-4, and EC6-3: request and authority publication `exactChangedPaths` arrays still omit exact minimum lengths and therefore accept a truncated two-path contract. | bad_spec |
| BH7-7 | high | The authority schema fixes `sourceBindings` at 29 rows but pins only the first 15 paths and permits generic trailing bindings, while runtime requires the exact 29-row inventory; schema-only consumers can substitute the fourteen successor-evidence bindings. | bad_spec |
| BH7-8 | high | carried BH6-4 and EC6-3: authority `gateDispositions`, `rootGitlinks`, and `assertionLedger` still omit exact minimum lengths, so incomplete authority inventories remain schema-valid. | bad_spec |
| BH7-9 | high | carried BH6-5 and EC6-3: the PASS result branch still accepts empty changed paths and a one-row authority ledger instead of the complete runtime inventories. | bad_spec |
| BH7-10 | high | carried BH6-5 and EC6-3: the request-BLOCKED result branch still accepts one ledger row and no blockers instead of the fixed runtime inventories. | bad_spec |
| BH7-11 | high | carried BH6-6: the FAIL schema branch still does not correlate its blocker identity and detail with the failed ledger assertion required by both protected hosts. | bad_spec |
| BH7-12 | medium | carried BH6-7: preservation measurement dictionary equality still accepts `mismatchCount: false` as numeric zero. | patch |
| BH7-13 | medium | carried BH6-9: performance summary validation still accepts `sampleCount: 30.0` because the generic numeric check and dictionary equality do not require an integer count. | patch |
| BH7-14 | medium | carried BH6-8: landing-zone measurement equality still accepts integral floats and `unresolvedCount: false` for required integer counts. | patch |
| BH7-15 | high | carried BH6-10: final pair validation still reads authority before architecture and never rereads authority, so a non-cooperating writer can mutate the first inode during the second read before success returns. | bad_spec |
| BH7-16 | high | carried BH6-11: publication still accepts multiply linked regular files, so an identical hard-linked authority retry or architecture path can modify an external alias. | bad_spec |
| BH7-17 | high | carried BH6-12: rollback still restores a quarantined pathname with replacing `rename`, which can overwrite a path recreated by another writer during recovery. | bad_spec |
| BH7-18 | high | carried BH6-13: Git committer time remains caller-controlled, so treating `%cI` as an independently trusted evidence/publication clock does not establish the claimed chronology. | bad_spec |
| BH7-19 | high | carried BH6-17: recovery validation still accepts the same generic two-word text for every exact procedure ID, so the operational contract remains token-shaped rather than phase-specific and actionable. | bad_spec |
| VG7-1 | high | carried VG6-1, pre-verified: candidate validation has no post-checkout assertion that `HEAD` equals the event-selected candidate and no runnable mismatch test. | patch |
| VG7-2 | medium | carried VG6-2, pre-verified: no test invokes `--write-request`, so the operator CLI can report success without creating the exact request artifact. | patch |
| VG7-O1 | high | carried BH6-1: the protected workflow installs dependencies into the project environment but invokes the setup-Python base interpreter, so `jsonschema` can be unavailable on a clean runner. | bad_spec |
| EC7-1 | false | carried BH-7: the canonical boundary is exact equality of the final request-to-source path set; reverted intermediate tree states do not alter that evaluated committed state, and the cited `V23_GATE_EVIDENCE_GRAPH_DRIFT` guard is not present at the reported location. | reject |
| EC7-2 | high | Gate and envelope instants are required to be at or after the committer instant of the commit that already contains those records; ordinary records timestamped before their containing commit are rejected, effectively requiring same-second or future-dated evidence. | bad_spec |
| EC7-3 | false | carried EC6-5: the frozen acceptance row's “current HEAD” explicitly names the historical `dcba5d4...` merge, which still returns `CANDIDATE_GRAPH_DRIFT`; it does not claim the successor request commit has that blocker. | reject |
| BH8-1 | false | carried BH4-1: the two changed specs are deliberate uncommitted review inputs, while committed `HEAD` contains exactly the nine tooling paths and the evidence ledger reports the two specs separately as worktree paths. | reject |
| BH8-2 | high | The protected hosts authenticate the publisher and workflow but not the candidate-owned V23 schema, while `validate_request` delegates the request's hold, owner/release/push, execution, and story-control fields to that schema. A loosened schema plus self-excluded request can therefore validate contradictory control claims even though the derived host envelope remains blocked. | bad_spec |
| BH8-3 | high | The same unpinned-schema dependency reaches signed authority records: runtime rederives gates, bindings, signature, ledger, and marker but does not independently require the authority's result, decision, hold, release/push, execution, and Story 7.1-only fields. A loosened tooling schema can admit an over-authorizing signed artifact while the returned envelope masks it. | bad_spec |
| BH8-4 | false | The approved proof model permits exact committed command-output bytes rather than requiring protected command replay; the validator parses `outputUtf8`, verifies its digest and zero exit, and recomputes each gate's canonical sources and measurements from those committed bytes. The finding would change frozen evidence semantics rather than correct this implementation. | reject |
| BH8-5 | false | The preservation record must enumerate the frozen denominator and all-PASS output, and the validator independently recomputes the denominator digest, category mapping, exact output, measurements, and per-source/per-test ledger. Requiring JUnit ingestion is outside the approved committed-output proof model. | reject |
| BH8-6 | false | carried BH3-7: performance raw samples, P95 values, regression, threshold, committed source/output bytes, and ledger rows are recomputed and signed; requiring an independently replayed benchmark is beyond the approved embedded-output proof model. | reject |
| BH8-7 | high | Effective OQ-1 successor grants copy obsolete predecessor `acceptedSourceSnapshot` values instead of binding EventStore and Commons to the protected request's current raw gitlinks (and Conversations to the frozen current source). This contradicts the required current-gitlink closure and can make stale-snapshot grants effective. | bad_spec |
| BH8-8 | high | The canonical runbook is exact, but the envelope's `backupProcedure`, `rebuildProcedure`, and `disasterRecoveryProcedure` remain independent two-word strings. Contradictory summaries can pass beside the runbook, so the promised mechanically cross-bound recovery contract is incomplete. | bad_spec |
| BH8-9 | high | Protected `uv` and Python invocations inherit ambient Python and loader variables; `-P` and `PYTHONNOUSERSITE` do not suppress `PYTHONPATH`, `PYTHONHOME`, or startup-module execution. The clean-runner test removes those variables instead of proving the required hostile-environment isolation. | bad_spec |
| BH8-10 | maybe-false | A same-app candidate check could matter only if external branch protection consumes the trusted verdict solely by name. The repository records rulesets, external check identity, and no-bypass enforcement as excluded, the public ruleset inventory is empty, and authenticated classic branch-protection state is unavailable; that external configuration would settle the claim. | defer |
| BH8-11 | maybe-false | A check-name migration deadlock likewise depends on an unobserved external required-check configuration. The introducing request is deliberately a fail-closed pre-land transaction reviewed under the existing route, and no repository-owned branch-rule migration consumer is present; authenticated branch-protection state would settle the claim. | defer |
| BH8-12 | high | The advertised hostile workflow harness checks pinned text/digests and runs copied helper behavior, but does not execute the materialization, synchronized venv, sanitized shell, protected interpreter, and result-checker path as one production-shaped flow. Trust-boundary wiring can regress while those copied tests remain green. | patch |
| BH8-13 | high | After exclusive authority creation, `fstat` and the initial single-link check occur before the rollback-protected write block. An `fstat` fault or concurrent hard link raises while leaving a visible empty authority path, violating recoverable pair publication and blocking retry. | bad_spec |
| BH8-14 | medium | `remove_owned_file` can rename successfully and then fail opening or fsyncing the quarantine, but its generic error omits the quarantine path. Operators lose the required preserved-object location even though the bytes remain on disk. | patch |
| EC8-1 | high | The pinned checkout action defaults `allow-unsafe-pr-checkout` to false for `pull_request_target`, so fork PR heads fail before the protected hosts run. The protected design must explicitly opt into candidate checkout only with the already-required no-candidate-execution isolation and a matching fork-path test. | bad_spec |
| EC8-2 | low | Performance validation accepts a computed regression up to `1e-9` percentage points above the exact five-percent limit. The approved contract is `<=5%`; deleting the extra epsilon is a direct boundary correction. | patch |
| EC8-3 | high | The concurrent-hard-link path reproduces BH8-13: the post-create link-count rejection is outside cleanup and leaves the incomplete authority visible. | bad_spec |
| EC8-4 | false | carried EC4-4: the replace-capable `no_clobber=False` branch has no production caller; both request and authority creation use the exclusive `no_clobber=True` branch, so the claimed request-path replacement success is unreachable. | reject |
| EC8-5 | false | carried EC7-3: the frozen matrix names `dcba5d4...` as the historical current merge and it still returns `CANDIDATE_GRAPH_DRIFT`; marker-free successor `HEAD` is a different request candidate and need not return that blocker. | reject |
| VG8-1 | medium | Pre-verified: the clean-environment test runs the repository's existing `.venv`, not a freshly materialized protected project synchronized and executed through the workflow-created interpreter, so the venv wiring is not covered end to end. | patch |
| VG8-2 | high | Pre-verified: the checkout-mismatch test executes a copied shell fragment rather than the workflow-owned gate, so weakening the real comparison can leave the runnable fault green. | patch |
| VG8-3 | high | Pre-verified: both signed-authority host tests mock `load_v23_publisher`; neither production host proves a valid signed authority through the real authenticated loader and topology. | patch |
| VG8-4 | medium | Pre-verified: malformed recovery content mutates only DETECT; exact action and outcome enforcement for CONTAIN, RESTORE, VERIFY, and ESCALATE can regress without a failing test. | patch |
| VG8-5 | high | Pre-verified: existing hard-link tests act before publication and the A1/B/A2 race test mutates bytes only; no fault creates a link between final observations to prove final single-link rejection and alias preservation. | patch |
| BH9-1 | medium | The trusted `pull_request_target` path is meaningful only for protected `main`, but the event currently admits every base branch and labels each base SHA as protected. Restricting that trigger to `main` closes the misleading non-main route without changing candidate execution. | patch |
| BH9-2 | false | carried EC6-1 and BH3-2: `workflow_dispatch` is an event-qualified diagnostic path, not the protected pull-request authority consumer, and its selected-ref result cannot authorize or merge `main`. | reject |
| BH9-3 | high | The production-shaped harness still injects comparison commits instead of executing the workflow-owned immutable-range block, so protected-base/head selection and trusted-host derivation can regress independently of the runnable test. | patch |
| BH9-4 | high | The harness stubs do not validate their exact CLI arguments and return a truncated synthetic result, allowing protected host argument wiring or closed-envelope checks to weaken while the integration test remains green. | patch |
| BH9-5 | false | carried BH6-16: the predecessor contract fixes these path names, and the protected loader validates their canonical JSON bytes; a `.md` suffix does not alter the frozen machine-readable representation. | reject |
| BH9-6 | medium | Owner rationale validation enforces length but not substance, so a repeated-character placeholder satisfies a field explicitly required to be non-placeholder. Reuse the substantive-text guard and add a negative fault. | patch |
| BH9-7 | false | carried BH5-6: the approved horizontal metrics are finite, exact, and ordered but deliberately do not impose a nonnegative domain; negative values do not by themselves contradict the closed scaling contract. | reject |
| BH9-8 | high | Publication validates inode type and link count but not the required non-executable mode on existing architecture or byte-identical retry paths, allowing a successful publication whose commit is rejected later for mode drift. | patch |
| BH9-9 | false | On an unverifiable post-create race, the approved recovery contract deliberately quarantines the governed visible name without overwriting or unlinking it and reports the preserved bytes; retaining an unverified visible authority would instead block retry and violate fail-closed pair recovery. | reject |
| BH9-10 | medium | When architecture restoration fails after quarantine, the direct `EntryAuthorityError` rethrow omits the quarantine location even though recovery requires that preserved-object path in every failure diagnostic. | patch |
| BH9-11 | low | The operator workflow and protected host are explicitly Ubuntu/Linux, and this story does not claim a portable publication CLI surface; adding a cross-platform locking and no-replace abstraction is outside the approved scope. | reject |
| BH9-12 | low | Malformed argparse syntax is rejected before the governed publication operation begins; the structured result contract applies to an invoked action, and replacing argparse's standard usage boundary adds no authority safety. | reject |
| BH9-13 | false | The human explicitly reset the fresh corrective build to iteration zero, and the sole bad-spec loopback in this build correctly incremented `review_loop_iteration` to one; historical triage identifiers are retained evidence, not the current loop counter. | reject |
| BH9-14 | false | The later accepted loop-8 correction expressly supersedes the earlier provisional committer-time wording, and the rederived implementation uses ancestry, owner attestation, and protected validation time rather than Git committer time. Editing the accumulated historical note would erase review provenance. | reject |
| EC9-1 | high | A complete V23 marker without a V23 request or authority currently falls through the legacy evidence route, allowing the evidence host to pass a candidate that the authority resolver blocks. Reject the orphaned marker before route validation and add the fault. | patch |
| EC9-2 | medium | Binary-float arithmetic computes the exact boundary `1.05` over `1` as slightly greater than five percent, so deletion of the old epsilon introduced rejection of a mathematically compliant measurement. Compare exact decimal inputs while retaining strict rejection above five percent. | patch |
| EC9-3 | high | The evidence host hard-codes `PASS` while validating a V23 authority result, converting an authenticated structured publisher FAIL/BLOCKED into a generic boundary error and losing the required stable state and blocker. Validate and propagate the authenticated non-PASS envelope. | patch |
| VG9-1 | false | The real baseline resolver is expected to reject the pre-land request transaction: frozen acceptance requires that request to remain `BLOCKED`/false until a separate authenticated owner authority exists. Making the protected authority job accept the request as an executable authority verdict would collapse the intentionally separate two-commit gate; candidate validation and the evidence host already validate the request artifact independently. | reject |
| BH10-1 | false | carried BH8-4: the approved proof model accepts exact committed output bytes, then independently parses and recomputes each closed gate result; requiring protected command replay would change the frozen evidence model. | reject |
| BH10-2 | false | carried BH8-5: the preservation validator binds the frozen denominator, exact all-PASS output, measurements, and per-test/per-category ledger; requiring JUnit ingestion is outside the accepted proof model. | reject |
| BH10-3 | false | carried BH3-7 and BH8-6: the performance validator binds the frozen source and committed raw samples, recomputes P95 and regression, and the signed authority binds the result; a replayed benchmark is outside the approved embedded-output model. | reject |
| BH10-4 | false | carried BH5-9: zero is a valid nonnegative timing, the baseline P95 must be positive, and the contract requires a canonical minimum sample count rather than equality with the baseline sample cardinality. | reject |
| BH10-5 | false | carried BH4-7: the immutable exact-transaction preservation manifest is the canonical category source, its category order and output projection are compared exactly, and its mappings intentionally cover governed subsets rather than the complete denominator. | reject |
| BH10-6 | false | The performance baseline is an immutable request-bound canonical blob; future authority validation requires its source identity to equal the request binding, so duplicate or non-object rows cannot be introduced at the cited route. | reject |
| BH10-7 | false | The approved chronology deliberately permits evidence instants to predate their containing commit and defines no maximum-age policy; ancestry, owner attestation, and the trusted validation upper bound are the governing freshness contract. | reject |
| BH10-8 | false | carried BH5-6 and BH9-7: finite ordered scaling thresholds may be negative for signed metrics; the frozen contract intentionally does not impose a nonnegative domain. | reject |
| BH10-9 | medium | `worktree_binding` follows symlinks and can block on a FIFO while declaring mode `100644`; request generation needs a no-follow, nonblocking regular single-link mode check before hashing prospective tooling bytes. | patch |
| BH10-10 | false | The cited runner-selected Python and pip-installed uv are confined to the unprivileged candidate-test lane; protected authority resolution uses the separately pinned candidate-free interpreter and uv identities required by the spec. | reject |
| BH10-11 | false | `--write-request` is intentionally no-clobber and creates the initially absent request over the tooling baseline; adding an update/replace mode would weaken the explicit request publication boundary. | reject |
| BH10-12 | false | The eight state-dependent V9 assertions are intentionally excluded only from the current-tree lane and run in the complete historical V9 checkout; current V23 behavior has its own successor-specific suites. | reject |
| BH10-13 | maybe-false | carried BH8-10 and BH8-11: whether required-check identity or migration is externally incomplete depends on authenticated branch-protection state unavailable to this repository; the two existing deferred records already name the evidence needed. | defer |
| EC10-1 | medium | The prospective tooling reader uses `Path.read_bytes`, so a FIFO can hang and a symlink can bind external bytes before committed mode validation; require descriptor-relative no-follow/nonblocking regular-file validation. | patch |
| EC10-2 | false | The ineffective predecessor grants have no effective instant, Git dates are explicitly untrusted, and the approved chronology permits evidence instants to predate their containing commit; successor effective time is correctly bounded by disposition and validation. | reject |
| EC10-3 | high | A descriptor-close failure in `atomic_write` occurs after exclusive authority creation but before the caller receives `authority_created` and its inode identity, leaving a visible unpaired authority that the outer rollback cannot identify. | patch |
| EC10-4 | high | `remove_owned_file` can successfully quarantine the owned inode and then let an unwrapped descriptor-close error override the return, losing both the stable rollback code and the preserved quarantine path. | patch |
| EC10-5 | medium | The request CLI prints success immediately after `atomic_write`; a pathname replacement after the write but before return is not revalidated, so success can name bytes no longer visible at the request path. | patch |
| EC10-6 | false | carried EC7-3 and EC8-5: “current HEAD” in the frozen acceptance row explicitly names the historical protected merge `dcba5d4...`, which still yields `CANDIDATE_GRAPH_DRIFT`; it does not describe the marker-free request commit. | reject |
| VG10-1 | high | Pre-verified: both contradictory-result mutation suites begin from an already-invalid one-row PASS ledger, so signer and other individual host guards can regress while every mutation case remains green. | patch |
| VG10-2 | medium | Pre-verified: the runnable workflow result-checker harness exercises only legacy non-executable PASS, leaving executable V23 PASS and structured FAIL/BLOCKED successor branches unproved. | patch |
| VG10-3 | high | Pre-verified: no raw-JSON fault injects duplicate control properties into request, authority, or gate evidence, so replacing duplicate-safe parsing with ordinary `json.loads` would leave the suite green. | patch |

## Verification

**Commands:**
- `uv run --frozen --no-sync python3 -m pytest -q -ra -o xfail_strict=true _bmad/scripts/tests/test_resolve_current_planning_authority.py _bmad/scripts/tests/test_verify_evidence_boundary.py _bmad/scripts/tests/test_publish_story_7_1_entry_authority.py` -- expected: exit 0, skip-free PASS with named PASS/FAIL/BLOCKED faults.
- `uv run --frozen --no-sync python3 _bmad/scripts/verify_evidence_boundary.py --repository . --baseline e0b098fa1c056385e28ee8ac0efd0c55dfab324f --candidate HEAD` -- expected for tooling: PASS with a nonempty ledger and exact nine-path boundary.
- `uv run --frozen --no-sync python3 _bmad/scripts/resolve_current_planning_authority.py --repository . --candidate HEAD --check` -- expected: PASS/true only after the separate valid owner publication; otherwise FAIL or BLOCKED/false.
- `git diff --name-status e0b098fa1c056385e28ee8ac0efd0c55dfab324f..HEAD` and `git ls-tree -r HEAD -- <nine tooling paths>` -- expected: exactly the declared nine paths, each mode `100644`, with no gitlink change.
- `git diff --check` -- expected: no whitespace errors in the scoped candidate.
