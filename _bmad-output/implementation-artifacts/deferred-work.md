# Deferred Work Ledger

Real findings that are not actionable in the story that surfaced them. Each entry records where it came from so a later sweep can verify it against the codebase before acting.

## Deferred from: code review of spec-6-1-rebaseline-architecture-and-planning-authority (2026-07-26)

- **The planning gate has no automated execution path.** Story 6.1's stated approach is to "enforce the resulting planning contract with focused conformance tests", but the repository has no `.github/workflows` directory and no pipeline definition anywhere outside `references/`. The story's Verification section lists hand-run binary invocations. Nothing causes `ArchitecturePlanningAuthorityValidationTest` (or the other 390 conformance facts) to run on any commit, and the artifacts it guards are markdown that later agents edit without triggering a build. Until this is wired into CI or a pre-commit hook, every authority assertion is advisory. Pre-existing repository condition, not caused by Story 6.1.

- **Nested submodule administrative metadata exists under a root submodule.** `.git/modules/references/Hexalith.FrontComposer/modules/Hexalith.AI.Tools` and `.../Hexalith.Builds` exist with mtimes 2026-07-14 22:34 and 2026-07-15 01:22. Both working trees are empty (0 entries), so the nested submodules are registered in git metadata but not checked out. The timestamps predate Story 6.1's implementation window, so this is not attributable to this pass, but it sits against the standing rule never to initialize or traverse nested submodules. A later sweep should confirm whether the metadata can be cleaned up without disturbing the FrontComposer checkout.

## Deferred from: code review of 6-7-mechanically-block-incomplete-submodule-promotions-from-completion (2026-07-27)

- **`bmad-dev-story` cannot detect an undeclared, uncommitted submodule promotion made during its own session.** It captures `baseline_commit` once at `ready-for-dev` and never runs `git commit` in any of its own steps; step 9 resolves "committed `HEAD`" as the gate candidate, so if nothing was committed, `HEAD` can equal `baseline_commit` and an undeclared/uncommitted promotion is invisible to the gate's blocking path. Deferred per Jerome's decision (2026-07-27 code review): requires both undeclared scope and leaving it uncommitted — narrow edge case, not worth changing the frozen four-workflow checker contract right now. Record as a known limitation in Dev Notes/runbook on a future pass.

- **`remote_contains()` has no staleness/shallow-clone signal for local remote-tracking refs.** `_bmad/scripts/verify_submodule_promotion.py` decides `REMOTE_COMMIT_UNAVAILABLE` purely from local `refs/remotes/*` containment, by design (never fetch). A force-pushed/rewritten remote, or a shallow submodule clone, can make a local ref falsely appear to contain (or not contain) a commit. Pre-existing tradeoff of the no-fetch-ever design, not a defect introduced incorrectly by this story.

- **Nested-gitlink dirt compensation in `submodule_dirt()` only covers the staged case.** An unstaged nested-submodule pointer change (checkout without `git add`) is invisible to both the status call and the `diff-index --cached` compensation. Nested submodules are policy-forbidden from ever being initialized in the first place, so this is defense-in-depth, not a live path today.

- **`ArchitecturePlanningAuthorityValidationTest.cs`'s new AppHost-qualification check is per-physical-line.** A future purely-cosmetic line-wrap of a qualifying phrase could produce a false conformance failure. Pre-existing test-authoring pattern in this file (substring/line-based checks), not unique to this story's addition.

- **`inspect_unrelated()` emits no warning at all for a fully-uninitialized unrelated root-declared submodule** (only initialized-but-dirty/drifted unrelated submodules warn). Not contradicted by the frozen warning-code table, but a silent blind spot worth a follow-up decision on whether uninitialized-unrelated should also warn.

- **Case-insensitive filesystem checks vs. case-sensitive git object matching.** A case-mismatched `--submodule` path could pass `own_worktree`'s filesystem check but fail the `git ls-tree`/`diff --raw` lookup with a misleading code. No Windows/macOS caller exists in this workspace today.

## Deferred from: code review pass 2 of 6-7-mechanically-block-incomplete-submodule-promotions-from-completion (2026-07-27)

- **An unstaged nested-gitlink move inside a declared submodule is reported `clean`.** `submodule_dirt()` (`_bmad/scripts/verify_submodule_promotion.py:307-337`) uses `git status --ignore-submodules=all`, which hides the change, and the compensating `git diff-index --cached HEAD` only sees the staged case. Reproduced: `clean: true`, exit 0 pass, while plain `git status` in the submodule shows ` M nested/Child`. This re-affirms the pass-1 defer (nested submodules are policy-forbidden from being initialized at all, so this is defense-in-depth). New information for a future sweep: `--ignore-submodules=dirty` would catch the unstaged case without initializing or traversing anything, so the fix need not weaken the non-traversal guarantee.

- **`own_worktree()` failure makes `inspect_unrelated()` return silently with no warning.** `_bmad/scripts/verify_submodule_promotion.py:494` returns early when `own_worktree()` is false, which covers not only the uninitialized case already deferred in pass 1 but also a submodule whose `.git` gitdir-file points at a deleted or moved gitdir. Reproduced: exit 0 `pass` with `warnings: []` — no `UNRELATED_SUBMODULE_INSPECTION_FAILED`, and `UNRELATED_GITLINK_DRIFT` is never evaluated, contradicting the guarantee stated at `docs/runbooks/submodule-promotion-completion-gate.md:113-115`. Extends the existing uninitialized-unrelated defer rather than introducing a new class.

- **Both `bmad-quick-dev` routes stage only *declared* gitlinks, so `UNDECLARED_GITLINK_CHANGE` is structurally unreachable on that path.** `step-05-present.md:55` and `step-oneshot.md:59` both say "Stage only the scoped implementation files, `{spec_file}`, and exact declared root gitlinks". An undeclared bump is therefore never committed, never appears in `changed_gitlinks`, and never activates the gate. Proven on a throwaway repo: with no `--submodule`, `result: pass`, `changed_gitlinks: []`, exit 0; with `--submodule refsub`, `result: blocked`, `GITLINK_COMMIT_MISMATCH`, exit 1. This is an inherent tension with non-negotiable constraint #5 ("stage only declared files; never `git add -A`"), which is itself a hard rule carried forward from the Stories 2.2 and 3.3 review corrections — resolving it means changing one of the two rules, which is an owner decision, not a defect fix.

- **Nothing executes the promotion gate automatically.** No `.github/workflows` exists outside `references/`, no git hook invokes the checker, and `grep -rn "verify_submodule_promotion\|submodule_promotions" tests/ --include=*.cs` returns nothing. The pytest suite is manual-only and requires `uv` (system `python3` has no pytest). AC3's "mechanically block review/completion workflows" therefore rests entirely on agent compliance with markdown prose. The story acknowledges this at Dev Notes → Git intelligence, and Story 6.1 already logged the same condition for the planning gate — recorded here so a single future sweep can wire both into CI together.

## Deferred from: 6-8-generate-the-final-story-record-mechanically-from-measured-state (2026-07-28)

- **Nothing executes the final-record generation gate automatically.** This is the third instance of the same standing condition already recorded above for the planning gate (Story 6.1) and the promotion gate (Story 6.7 pass 2). `_bmad/scripts/generate_story_record.py` is invoked only by markdown prose in four skill files; no `.github/workflows` exists outside `references/`, and no git hook runs it. `StoryFinalRecordGenerationValidationTest` does run in the conformance lane and proves the invocation prose is still present and still enforcing, which is strictly more than the promotion gate has — but it proves the prose exists, not that anything executed the generator. The pytest suite `_bmad/scripts/tests/test_generate_story_record.py` remains manual-only and requires `uv` (system `python3` has no pytest). A single future sweep should wire all three gates into CI together.

- **`bmad-dev-auto/step-04-review.md` reaches `done` without a generated record.** It is the fifth entry in `WORKFLOW_GATE_CONTRACTS`, has a promotion completion gate, and writes frontmatter `status: done`, but the frozen Epic 6 authority says "the four completion surfaces" and names four — `bmad-dev-story` step 9, `bmad-quick-dev/step-05-present.md`, `bmad-quick-dev/step-oneshot.md`, and `bmad-code-review/steps/step-04-present.md`. Story 6.8 deliberately did not add it (Dev Notes D5): widening frozen acceptance scope is an owner decision, not a dev-agent call. This is therefore a live bypass route to `done` with a hand-authored record. If the owner authorises the expansion it is a fifth entry in the surface list and in both non-deletability guards, not a redesign.

- **Four artifact defects found while preparing Story 6.8, not fixed by it.** All four are authority edits, outside a dev-agent call. (1) `epic-6-context.md:8` `source_overlay_begin` still names the v3 marker `EPIC-6-AUTHORITY-OVERLAY-AMENDMENT:BEGIN` while `overlay_version` is v4, whose real marker is `EPIC-6-AUTHORITY-OVERLAY-AMENDMENT-V4:BEGIN` — the context declares provenance from the block it supersedes. (2) `epic-6-context.md:140` drops three prohibition categories the frozen text carries (package versions, generated output, accepted baselines); Story 6.8 quoted the frozen text verbatim in its own record to neutralise this for itself, but the context stays wrong for the next reader. (3) `architecture.md:1548-1550` `Corrective Implementation Handoff` still states the pre-v4 sequence with no mention of 6.8, contradicting `:193` in the same document. (4) `_bmad/render/bmad-quick-dev/` and `_bmad/render/bmad-dev-auto/` are derived, stale snapshots that Story 6.7 decided not to edit, but commit `5ed5e20` modified five of them, so the "untouched" state 6.7 recorded no longer holds. Story 6.8 did not edit them either; the two skill trees under `.claude/skills/` and `.agents/skills/` are the live surfaces and both were updated.

- **Staleness is an mtime comparison, so a content-preserving mtime rewrite can produce a false positive.** `generate_story_record.py` blocks `TEST_RESULTS_STALE` when an artifact's mtime predates the newest file in the derived list. A checkout, `cp` without `-p`, or an editor that rewrites a file without changing its bytes can therefore trip it. Re-running the tests is always a valid remediation and can never hide a real staleness, so this is a usability cost rather than a correctness hole — but a content-hash-based input fingerprint (the Epic 5 asset's `Get-InputFingerprint` approach) would remove the false positive entirely.

## Deferred from: spec-gh-actions-30291329462-90061623240 (Hexalith.Tenants release version floor, 2026-07-28)

All items are in `references/Hexalith.Tenants`; surfaced by the review layers over the floor change, not caused by it.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-actions-30291329462-90061623240.md`
  summary: `recover-partial-release.yml` bypasses the publication version floor entirely, and its documented `3.3.0` example would mix package lines.
  evidence: `scripts/validate-partial-release-recovery.sh` and `publish-partial-release.sh` never call `validate-publication-preflight.sh`; their only version check is a SemVer regex. The workflow input description names `3.3.0`, whose Contracts/Server/Aspire packages were published 2026-05-02 from unrelated source — run 30291329462 published nothing. Dispatching recovery for it would ship Client/Testing `3.3.0` from current source alongside three May-2026 packages at the same version. Also, `publish-partial-release.sh` gates on `gh release view`, not tag existence, so `gh release create --target` would silently ignore its target on an existing tag.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-actions-30291329462-90061623240.md`
  summary: The floor is a static constant, so the same incident recurs silently once the version line is inside 4.x.
  evidence: `minimum_release_version=4.0.0` never rises. After 4.9.0 ships, deleting tags again makes semantic-release resume at a 4.0.x proposal — above the floor, so the guard passes and the collision returns at push time. A floor derived from the highest published NuGet version or highest reachable release tag would track it.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-actions-30291329462-90061623240.md`
  summary: Nothing fails in CI when the release-config option names or the tag line drift.
  evidence: `PackageGovernanceTests.cs` pins `successCommentCondition`/`releasedLabels`/`labels` as JSON substrings, but `package.json` allows `@semantic-release/github` `^12.0.8`, so a renamed option in a later major keeps the suite green while the write returns. Likewise no test asserts that the highest reachable semver tag is at or above the floor, so a repeat tag deletion is invisible until a release is dispatched.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-actions-30291329462-90061623240.md`
  summary: `CHANGELOG.md` is frozen at 3.2.18 and will never record 4.x.
  evidence: `@semantic-release/changelog` and `@semantic-release/git` were deliberately removed in 6cc9eb3 (the git plugin pushes to main during prepare and strands the publish-phase source proof), and `PackageGovernanceTests.cs` asserts their absence. Release notes live only in GitHub Releases. Either freeze `CHANGELOG.md` with a pointer or restore generation on a path that cannot advance main mid-release.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-actions-30291329462-90061623240.md`
  summary: `CONTRIBUTING.md` still tells contributors that merging to `main` releases automatically.
  evidence: Step 7 of the Pull Request Process predates 6cc9eb3, which replaced `workflow_run` with `workflow_dispatch` plus an operator-owned `verify-source` gate. Releases now require an explicit dispatch.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-actions-30291329462-90061623240.md`
  summary: The preflight's SemVer regex admits leading zeros, which `sort -V` then mis-ranks.
  evidence: `04.0.0` passes `^[0-9]+\.[0-9]+\.[0-9]+...` and is rejected as below the `4.0.0` floor. Unreachable from semantic-release, which never emits such a version, so the regex was left as-is rather than tightened inside a release-gate fix.

## Deferred from: code review of 6-8-generate-the-final-story-record-mechanically-from-measured-state (2026-07-28)

- **Non-declared submodules that move after the candidate never trip `CANDIDATE_NOT_FINAL`.** `generate_story_record.py:1009-1020` computes `moved` for every root gitlink but only blocks for paths in `affected` (changed-in-range ∪ declared). Under `-p:UseHexalithProjectReferences=true` any root submodule re-checkout changes the compile inputs, so an undeclared submodule moving after the candidate invalidates the counts invisibly. The story hit this exact failure during its own implementation (Debug Log: the 15:35 measurement invalidated by a 17:50 re-checkout) and discloses it as a residual limitation. `gitlinks_moved_after_candidate` is already computed and sitting in the document, so extending the check is cheap — the open question is whether blocking on undeclared movement would make the gate unpassable in a workspace where concurrent sessions move gitlinks freely.

- **The C# conformance guard is hard-pinned to Story 6.8's own record path.** `StoryFinalRecordGenerationValidationTest.cs:28-29` hard-codes `StoryPath` to the 6.8 story file, and `:203-224` derives its byte-identity path set from that story's frozen File List. A skill file added or changed by any later story can diverge between `.claude/skills` and `.agents/skills` with no test covering it, and renaming or archiving the 6.8 story file reddens the whole Conformance lane for a filing change. The three mutation tests (`:138`, `:154`, `:176`) also run only against `AgentTree`, so a `.claude/skills` mutation is unproven.

- **The pytest suite hard-binds to live repository history despite T7's hermeticity claim.** `test_generate_story_record.py:682-688` pins three real closed records; `:691-701` asserts classification and result against the live workspace; `:704-718` asserts `len(derived) == 37` against a real commit range. Any future edit adding the literal string `story-final-record-v1` to one of those three records flips its classification, turns its warnings into blockers, and reddens the suite for a documentation change. T7 claims "everything under `tmp_path`".

- **`HEAD` is resolved independently at three points with no snapshot.** `worktree_path_status:505-515` runs `diff-index --cached HEAD`, `evaluate_candidate_binding:982` runs `rev-parse HEAD`, and the embedded promotion checker resolves its own at `:1173`. A commit landing between them yields one document describing two heads. This is the same concurrency hazard the story's Debug Log records twice; reasoned from the code rather than reproduced, and the window is short, but the workspace demonstrably has concurrent sessions committing.

- **`tests/README.md` retires the PowerShell manifest's immutability and contract-shape assertions with no equivalent in the replacement.** The removed block carried "unchanged exact pre-existing file hashes and gitlink/index/worktree identities", "required JSON/Markdown pairs", and "the non-mutating full public-contract-shape comparison and unchanged baseline artifact". The generator implements none of these, and the same hunk states the retained PowerShell asset "is not invoked by any workflow" — so those checks are now unenforced rather than superseded. Needs an owner call: re-establish them, or state in the runbook's Known limitations that they were retired with the hand-authored manifest.

- **File List annotations are not reproducible from `file_list_commit`.** `derive_file_list:898-899` lets worktree status override the committed-range status, so the annotation depends on transient dirtiness at generation time rather than on the bound revision. The shipped block annotates the story file `(modified)` where a re-run yields `(new)`. Paths reproduce exactly; only the parenthetical drifts. Related: a file added in the range and later deleted in the worktree is annotated `(deleted)` in a record binding to a revision where it exists.

- **The generated Gitlink Promotions table presents inherited affected scope as this story's promotions.** All three declared gitlinks render with `Declared: yes` and the embedded gate line lists them as `declared / changed / evaluated`. Nothing inside the verbatim block distinguishes scope this story inherited from concurrent commits (`e74c09a`, `f954202`) from a promotion the story performed — Story 6.8 modified no submodule source and committed nothing inside any submodule. The disclaimer lives only in surrounding prose, which the frozen authority says may not carry record semantics. A future consumer reading generated blocks mechanically (Story 6.6's attestation) would attribute three promotions to Story 6.8.

- **The runbook's absolute non-mutation claim is checked by one hash of one file in one mode.** `docs/runbooks/story-final-record-generation.md:192-194` claims the generator never mutates repository state "in either mode"; the only evidence is `test_generate_story_record.py:696`, which hashes a single closed record around a `--historical` run. No test snapshots `git status` around a live run, and nothing pins the verb allowlist passed to `run_git`. The property holds by construction today; the guard against regression is absent.

- **Story 6.8's own record is excluded from the `CLOSED_RECORDS` re-derivation test it wrote for its siblings.** `test_generate_story_record.py:684-689` contains 6.1, 6.2 and 6.7 only, and 6.7 gets a hard re-derivation assertion (`:707-717`). 6.8 has no analogue, so the record's headline numbers have no enforcing assertion. Cannot be added until the story closes; add it as part of closing.

## Deferred from: code review of 6-2-migrate-conversations-to-platform-owned-hosting (2026-07-29)

- **The tenant index remains a single state-store value and now also carries a per-conversation dispatch map.** `ConversationProjectionIndexReadModel.cs:26-31` holds every `ConversationSummaryProjectionV1` for a tenant plus a `Dispatches` entry per conversation under one key, written through `ReadModelWritePolicy.MergeAsync` (`ConversationProjectionReadModelWriter.cs:96-109`). There is no size guard or overflow strategy. A tenant crossing the state store's per-item ceiling makes the index merge fail permanently, surfacing as `Retryable` on every attempt with no path forward. Deferred as a pre-existing single-key index design that Story 6.2 extends rather than introduces; the dispatch map raises the per-tenant ceiling sooner than before.

- **`SameSummary` defines equality over serialized form.** `ConversationProjectionReadStore.cs:273-276` compares summaries with `JsonElement.DeepEquals(SerializeToElement(first), SerializeToElement(second))`, so any member excluded from serialization (`[JsonIgnore]`, non-public) compares equal and a genuinely divergent summary passes the content-mismatch guard that `ConversationProjectionReadStoreFailClosedTest.SummaryContentMismatchShouldReturnRebuilding` claims to enforce. It also allocates two `JsonElement` trees per summary on the read path, once per row in `ListAsync`. Deferred as a pre-existing comparison approach; the new fail-closed guarantees inherit its blind spot rather than create it.

## Deferred from: code review of 6-2-migrate-conversations-to-platform-owned-hosting.md (2026-07-30)

- **The final-record generator invocation check is not scoped to the bounded completion-gate body.** `StoryFinalRecordGenerationValidationTest.cs:128-139` accepts the generator command anywhere in a workflow file once the gate markers are present. The corresponding Python mutation test in `_bmad/scripts/tests/test_generate_story_record.py:1318-1338` has the same global-presence weakness. A future workflow could move the invocation outside the enforcing gate and keep both guards green. This is a pre-existing Story 6.8 conformance defect, not part of Story 6.2's evidence implementation.

- **The bmad-dev-story completion dependency guard is vacuous because the dependency and follower markers are identical.** `StoryFinalRecordGenerationValidationTest.cs:40-58` configures the same text for both fields, and `:346-352` can therefore satisfy the positive dependency check with the follower itself. The current workflow ordering is correct, but the test cannot prevent a future bypass. Give the contract a distinct predecessor marker and assert its order inside the bounded gate. This belongs to Story 6.8's final-record workflow contract.

- **Dual skill-tree parity coverage trusts a hard-coded contract list.** `StoryFinalRecordGenerationValidationTest.cs:272-284` compares only files named by `Contracts`; a newly added completion surface can diverge between `.claude/skills` and `.agents/skills` without entering the comparison. Derive the relevant surface set from both trees and fail on additions, removals, or byte differences. This is a pre-existing Story 6.8 guard limitation.

- **The explicit review-commit authorization guard checks presence but not ordering.** `StoryFinalRecordGenerationValidationTest.cs:148-158` does not prove that authorization occurs before candidate preparation and both completion gates. A reordered workflow could prepare or validate an unauthorized candidate while retaining all required text. Add bounded ordering assertions in the Story 6.8 workflow conformance guard.

- **The Story 6.8 conformance guard permits an empty generated File List.** `StoryFinalRecordGenerationValidationTest.cs:291-299` proves the heading count and filters parsed references, but never requires at least one entry. A malformed or empty generated list can therefore pass that portion of the guard. Require a non-empty canonical entry set before validating paths. This is pre-existing final-record validation work rather than a Story 6.2 evidence change.

## Deferred from: code review pass 7 of 6-2-migrate-conversations-to-platform-owned-hosting (2026-07-30)

- **The generator invocation surface is proven by substring presence in Python source.** `StoryFinalRecordGenerationValidationTest.cs:111` (`GeneratorMustExistAndExposeTheContractedInvocationSurface`) asserts `generator.ShouldContain("\"--story\"")` and siblings, which pass if the flag string survives in help text, a comment, or dead code after the argparse registration is deleted. Nothing executes the generator or even its `--help`. For a gate whose thesis is "mechanical, not textual", the mechanism's own contract check is textual. Joins the pass-6 Story 6.8 guard set — fix together by executing `--help` (or parsing the argparse table) in the guard.

- **The File List guard's parser and heading counter disagree on line endings and bullet shape.** `StoryFinalRecordGenerationValidationTest.cs:295` counts `"\n### File List\n"` LF-only while the parser normalizes CRLF, so a CRLF checkout fails the exactly-one-heading assertion on a correct record; and the entry parser accepts only lines beginning exactly ``- ` ``, so an indented or differently bulleted `references/...` path evades the submodule-path prohibition. Same Story 6.8 guard family as the pass-6 deferred set. _(Restored to its originating pass-7 section on 2026-07-31: the pass-8 heading had been inserted above this bullet rather than below it, so a pass-7 finding was attributed to pass 8.)_

## Deferred from: code review pass 8 of 6-2-migrate-conversations-to-platform-owned-hosting (2026-07-30)

All entries below are Story 6.8 deliverable defects surfaced while reviewing Story 6.2's workflow/planning
range; they are routed to Story 6.8's in-progress review cycle rather than patched cross-story. Authoritative
detail lives in the 6.2 record's pass-8 Deferred section.

- **Embedded promotion gate ignores `SCOPE_NOT_EVALUATED`** (`generate_story_record.py` maps only `result != "pass"`), so a durable record can embed a vacuous PASS the workflow surfaces would fail — reachable with empty declarations and `--baseline` equal to the candidate.
- **Single-quoted frontmatter scalar with trailing comment fails revision resolution** (`parse_yaml_scalar("'sha' # comment")` keeps the quote), yielding false `BASELINE_NOT_TRUSTWORTHY`/`CANDIDATE_NOT_FINAL` on correct fields.
- **Uncommitted gitlink drift in an undeclared submodule is invisible to every record guard** — `--ignore-submodules=all` porcelain, mtime-only staleness, and committed-range candidate binding all miss a worktree-level pointer move that changes compile inputs under `-p:UseHexalithProjectReferences=true`.
- **Untested generator branches:** `--require-remote` pass-through to the embedded checker and the sprint-status output-target allowance are executed by no pytest; all TRX parsing is pinned by synthetic `write_trx` fixtures only.
- **Parsing/edge batch:** UTF-8 BOM voids frontmatter; a root-level story derives `./sprint-status.yaml` (never matches a git path); YAML block-scalar indicators pass as non-empty skip reasons; `"**Final record** —"` substring anywhere flips historical classification to `malformed-generated`; live/historical modes disagree on a test skipped in two projects; markers quoted in story prose hijack `record_anchor`; `HEAD` is resolved at three unsnapshotted points per run; the legacy 7-column shim fabricates `executed = total − skipped`; `pre_parse_output_format` honors the first `--format` token while argparse honors the last.
- **Textual enforcement gaps:** `completion_surface_violations` accepts required substrings anywhere in a file and the gate-span test walks `.agents/skills` only; `bmad-code-review` step-04 candidate step 5 names no blocker/HALT on violation and contradicts the record gate's TRX-dirt allowance; the "insert between `### File List` and `### Boundary Confirmation`" wording invites a duplicate heading the next run blocks on with remediation text that forbids the escape edit.
- **Story 6.8 record bookkeeping:** the blocker/warning code tables are stale against the shipped generator (constraint #5 requires same-change table updates); three resolved first-pass decision checkboxes were never reconciled; `allowed_skipped_tests` still blesses the now-mandatory AppHost runtime boundary lane.

## Deferred from: code review pass 9 of 6-2-migrate-conversations-to-platform-owned-hosting (2026-07-31)

Surfaced while reviewing Story 6.2's promoted submodule ranges — the chunk passes 5 through 8 each deferred
as "promoted submodule ranges remain a separate review chunk". Authoritative detail lives in the 6.2 record's
pass-9 Deferred section. Entries touching `references/` are defects in promoted sibling code, recorded here
for the owning module rather than patched cross-repository.

- **`RepositoryProjectPaths` downgrades a correctness invariant to a comment, justified against the wrong repository.** The removed doc text asserted the probe order mirrors `$(Hexalith*Root)` precedence, which is what guaranteed the AppHost never compiles one csproj and launches another. The replacement NOTE admits candidate 4 (`<root>/Hexalith.<Module>/`) outranks `references/` and defends it with "No such directory exists in this repository" — but *this repository* is EventStore, while the consumer that drives the code is Conversations (`ConversationsAppHostTopology.cs:79`). Already tracked as EventStore Story 3.3 deferred work; fix by evaluating candidates mechanically or failing when more than one exists.
- **AC5 release evidence is bound to superseded commits on every axis.** `projection-read-store-population-proof-v2.json` pins candidate `b261fe20`, `eventStorePromotion.commit` `defb426f`, Tenants `b0451298`, Memories `4a6f0d33`; none matches the current recorded (`a40ab8a`/`33abe27`) or promoted (`e4618d9`/`625061b`) values. Subsumed by the three red evidence guards already pending regeneration.
- **`require_remote: true` verifies a local remote-tracking cache, not the remote.** `verify_submodule_promotion.py:469` runs `git for-each-ref --contains` over `refs/remotes/` with no fetch, and the conformance guard's supposedly independent re-derivation issues the identical local query. A branch force-pushed backwards or deleted upstream still reports `remote_available: true`, so nothing distinguishes "reachable upstream" from "our cache happens to contain it". Needs an out-of-band reachability check recorded in the evidence, or a warning code marking the answer cache-derived.
- **No CI job or test executes the promotion gate at the live candidate.** Conversations has no `.github/workflows` directory at all, and the only mechanical trace of the gate is a hand-transcribed JSON blob asserted with hardcoded literals from a run at superseded candidate `b261fe2` (`ProjectionReadStorePopulationProofValidationTest.cs:171`). An additional undeclared gitlink moving today changes no assertion. Needs a guard that re-derives `changed_gitlinks` at HEAD and requires the undeclared set to equal a disclosed allowlist.
- **`InMemoryReadModelStore` cannot model expiry, so every TTL-dependent edge is untestable.** `TrySaveWithTimeToLiveAsync` records `LastTimeToLive` and delegates to `TrySaveAsync`; entries never expire, the nested batch accessor does not override the TTL overload at all (so batch-path TTL is silently dropped in every test), and `LastTimeToLive` is a single unsynchronized global slot shared across all store names and keys. This makes the dedupe-window, post-expiry verification, and ledger-resurrection branches unreachable in tests. Needs `TimeProvider`-backed expiry honored on read.
- **`/project/v2/reconcile` is consumed but exercised at no boundary.** Conversations implements `IAsyncDomainProjectionReconciliationHandler` (`ConversationAsyncProjectionHandler.cs:33`), yet the route appears nowhere in `src`, `tests`, or `docs`. The host-composition route theory asserts six endpoints and not this one; all three reconcile tests call the handler method directly; the gateway lane never populates a terminal-route set, so `ReconcileTerminalRoutesAsync` never fires. Leaving the endpoint unmapped, or letting the route-catalog fingerprint diverge, would 400 every reconciliation with no test going red. Needs a live reconcile lane.
- **The dispatcher's new empty-history rejection is shadowed by a handler-level test that bypasses it.** `ValidateRebuildEventHistory` now rejects `events.Count == 0`, but Conversations' only empty-rebuild assertion calls `PrepareRebuildAsync` directly, which the dispatcher never reaches because it rejects before routing to handlers. Reverting the clause fails no Conversations test. Needs a `DomainProjectionDispatcher.RebuildAsync` assertion with an empty event array.

### Routed to Hexalith.EventStore from pass 9 (2026-07-31)

These are defects in promoted EventStore code that Conversations consumes. They are NOT applied in this
repository: editing a declared submodule dirties it (`SUBMODULE_DIRTY_TRACKED`) and blocks the promotion
gate. Each is stated with the evidence gathered during pass 9 so the owning repository can act without
re-deriving it. Conversations consumes the fix by advancing the EventStore gitlink once it lands upstream.

- **[HIGH]** The promoted `AddDataProtection()` registers a per-replica ephemeral key ring with no application name and no key persistence, while Conversations relies on it for list-cursor integrity and registers no Data Protection of its own — the same repository's own helper does `AddDataProtection().SetApplicationName(applicationName)` (`EventStoreDataProtectionServiceCollectionExtensions.cs:72`). Cursors minted by one replica cannot be unprotected by another and every restart invalidates outstanding cursors, against the binding rule "Conversation URLs/permalinks that encode temporal cursors must re-resolve identically" (`project-context.md:136`) (`references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/EventStoreDomainServiceExtensions.cs:343`)
- **[HIGH]** Reconciled terminal routes settle the retry ledger without advancing the idempotency record or the delivery checkpoint — `ReconcileTerminalRoutesAsync` results feed only `terminalRoutes.ExceptWith(...)` / `settledRoutes.UnionWith(...)`, while the normal success path calls `idempotencyCoordinator.CompleteAsync(...)` (`:557`) and `checkpointStore.SaveDeliveredSequenceAsync(...)` (`:570`). A route that goes `Failed` → reconciled `Completed` leaves EventStore believing the sequence never delivered, so a later at-least-once redelivery is admitted as new work; the only remaining dedupe is Conversations' TTL-bounded ledger, which compounds with the finding below (`references/Hexalith.EventStore/src/Hexalith.EventStore.Server/Projections/NamedProjectionDispatchCoordinator.cs:613`)
- **[HIGH]** Operation TTL entered the frozen-v1 batch fingerprint, so changing `RedeliveryWindow` bricks every in-flight resumable batch — Conversations passes `_ledgerTimeToLive` (`= options.RedeliveryWindow`) as the dispatch-ledger operation's TTL, and that value is now hashed into the fingerprint while `Version` still reads v1. An operator edit, or a rolling deployment briefly running two replicas with different values, makes a resumed batch compute a different fingerprint for the same logical dispatch → durable `IdentityConflict` that never self-heals. TTL is a storage-retention hint, not value identity (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/ReadModelBatchFingerprint.cs:55`)
- **[MEDIUM]** The batch marker is written with the no-TTL overload while its ledger operation carries a TTL, so post-expiry staging verification reports `Indeterminate` forever — `VerifyOperationKeysAsync` returns false for any write op whose key no longer exists, and the marker outlives the expiring ledger key. Either exclude TTL-bearing operations from verification or give the marker a TTL at least equal to the maximum operation TTL (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/ReadModelBatchProtocol.cs:432`)
- **[MEDIUM]** `GetManyAsync` degenerates to serial 1+N round trips exactly when batches are in flight — every envelope-wrapped key awaits `ResolveVisibleAsync` one at a time inside the loop, ignoring the `parallelism` argument, and each such call itself issues two reads. A 100-key chunk under an active batch costs up to 201 sequential round trips, reintroducing the N+1 the project rules forbid (`project-context.md:139`) (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/DaprReadModelStore.cs:113`)
- **[MEDIUM]** A duplicate key in the Dapr bulk response crashes the whole page with an unmapped `InvalidOperationException` from `.Single()`, pre-empting Conversations' own clearer duplicate diagnostic; use `First()` with an explicit conflict check or a typed exception naming the key (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/DaprReadModelStore.cs:99`)
- **[MEDIUM]** One corrupt row poisons an entire bulk page — the per-key `Deserialize<TValue>` has no try/catch, so a single schema-evolved or partially written row aborts `GetManyAsync` and takes a whole conversation list page down, where the single-key path would degrade one row. This contradicts the rule that projection reads surface degraded state rather than failing wholesale (`project-context.md:134`) (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/DaprReadModelStore.cs:117`)
- **[MEDIUM]** `ReadModelBulkEntry.ETag` is documented as "null when absent" but can be `string.Empty` for envelope-wrapped reads, conflating "absent" with "no stable ETag, batch in flight" — a caller treating non-null as usable performs a CAS with `""` and silently gets create-only semantics. Normalize at the bulk boundary or model the three states explicitly (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/IReadModelBulkStore.cs:30`)
- **[MEDIUM]** `IReadModelExpiringStore` and `IReadModelBulkStore` fall back to `DaprReadModelStore` when the registered `IReadModelStore` does not implement them, so a host or test that substitutes only `IReadModelStore` silently splits reads and TTL/bulk writes across two different backing instances. Throw at registration instead of falling back (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Registration/ReadModelStoreServiceCollectionExtensions.cs:40`)
- **[MEDIUM]** The new `RedeliveryWindow >= RetryMaxDelay + RetryLeaseDuration` cross-field rule can make a previously valid configuration fatal, and it surfaces as a 500 at request time rather than a startup failure — `options.Validate()` runs inside `ReconcileAsync` and the endpoint catches only `ProjectionDispatchValidationException`, so `ArgumentOutOfRangeException` escapes. The same throw also fires from Conversations' handler constructor at DI resolution. Validate once at startup via `IValidateOptions<T>`, and bound both delays before the addition to avoid `TimeSpan` overflow (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/ProjectionDispatchOptions.cs:123`)
- **[MEDIUM]** An empty rebuild history is now rejected as `MalformedOutcome`, a terminal non-retryable code that misattributes a well-formed empty request as malformed — the change is undocumented in the promoted range, and operators debugging a rebuild get a misleading reason. Use a distinct reason code and confirm no Conversations path (redaction, retention purge) legitimately presents a zero-event prefix (`references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/DomainProjectionDispatcher.cs:647`)
- **[MEDIUM]** `DomainProjectionRebuildRejectedException.ReasonCode` bypasses the reason-code bound applied to every other outcome — the catch constructs `ProjectionDispatchOutcome` directly, skipping `NormalizeOutcome` → `IsValidReasonCode` (length and ASCII), so a long or non-ASCII code escapes into the response envelope where the coordinator rejects the whole outcome, turning a precise terminal rejection into an unexplained indeterminate (`references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/DomainProjectionDispatcher.cs:472`)
- **[MEDIUM]** Terminal routes are reconciled forever for any domain that has not adopted the seam — a handler that does not implement `IAsyncDomainProjectionReconciliationHandler` returns `UnsupportedCapability`, and routes leave the set only on `Completed`/`AlreadyCompleted`, so every retry scan issues a fresh service invocation for work that can never settle. Conversations implements the seam, so this is a platform defect Conversations promotes onto its siblings; bound it with a reconcile-attempt count (`references/Hexalith.EventStore/src/Hexalith.EventStore.Server/Projections/NamedProjectionDispatchCoordinator.cs:726`)
- **[MEDIUM]** The "host stays authoritative" guards for `AddDaprClient` and `AddDataProtection` are order-dependent and their comments overstate them — both inspect only registrations made before the call, and Dapr's `AddDaprClient` uses `TryAddSingleton`, so a host configuring it *after* `AddEventStoreDomainService` silently loses its serializer/endpoint configuration to the SDK's unconfigured client. The comment claims the opposite for a call order nothing enforces or tests (`references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/EventStoreDomainServiceExtensions.cs:334`)
- **[LOW]** Consolidate the three divergent TTL-to-seconds conversions — `DaprReadModelBatchStateAccessor.ToTtlSeconds` (string, validates), `DaprReadModelStore.ToTtlSeconds` (long, validates), and the inlined `checked((long)Math.Ceiling(...))` in the fingerprint (no validation). Any future divergence changes the fingerprint relative to the value actually written, and the fingerprint is the frozen contract. Sub-second TTLs also collapse to one second and become fingerprint-identical, so reject TTLs below one second at the boundary rather than rounding (`references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/ReadModelBatchFingerprint.cs:55`)
- **[LOW]** The Aspire domain module now takes an unconditional `References`/`WaitFor` dependency on the EventStore resource with no opt-out and no note in the promotion, so every Conversations local run blocks until EventStore reports healthy; assert no reverse `WaitFor` edge exists in the AppHost compositions that use this extension, or the two deadlock at orchestration time (`references/Hexalith.EventStore/src/Hexalith.EventStore.Aspire/HexalithEventStoreDomainModuleExtensions.cs:73`)

## Deferred from: code review pass 10 of 6-2-migrate-conversations-to-platform-owned-hosting (2026-07-31)

- **[MEDIUM]** The AppHost lane's tenant-seeding step has no positive proof of effect. `SeedTenantAccessProjectionAsync` asserts only HTTP 200, and `MapProcessingResult` returns `Results.Ok()` for `SkippedUnknownEventType`, `SkippedNoHandlers`, `SkippedAggregateMismatch`, and `FailedInvalidPayload` — so a renamed `Hexalith.Tenants.Contracts.Events.TenantCreated`, a drifted payload shape, or the hard-coded `Role = 3` ceasing to mean `TenantReader` all pass, and the regression surfaces minutes later as an unattributed `PollForProjectedReadModelAsync` timeout that blames the projection boundary. Pass 10 attempted a probe twice and removed it both times rather than ship a broken strengthening: the `conversation-detail` query cannot be used because its denial shapes are deliberately indistinguishable (unauthorized, cross-tenant, and nonexistent all return `Forbidden` — see `DetailDenialPathsShouldShareSameShape`), and the `conversation-list` retry returned HTTP 500 because that handler ignores `aggregateId` and deserializes `query.Payload`, which the shared `SubmitQueryAsync` helper sends as empty bytes. A working probe needs the list query's real payload envelope (a serialized `ListConversationsPayload`), after which an admitted tenant reads `Current` with zero rows and an unadmitted caller reads `Forbidden` (`tests/Hexalith.Conversations.AppHost.Tests/ConversationsAppHostRuntimeBoundaryTest.cs:283`)

- **[HIGH]** Projection freshness is not reproducible across a full replay, and the instability leaks into documented domain content. `ConversationProjectionMaterializer.cs:127` computes `builder.LastAppliedTimestamp ?? projectionGeneratedAt`, so when replayed events carry no usable timestamp the rebuilt projection stamps **wall-clock time** instead of an event-derived one. Measured live on 2026-07-31: after derived-state deletion and full replay, `LastAppliedEventTimestamp` differed from the pre-deletion value. It does not stop at the freshness field — `:324` and `:382` resolve participant and file-reference `occurredAt` as `participant.OccurredAt ?? freshness.LastAppliedEventTimestamp`, so every item whose own `OccurredAt` is null inherits the same unstable value into a field the projection contract documents as domain content ("the projected time the participant became part of the record"). Consequence: the AC6 release evidence asserts `queryResultsEquivalentToPreDeletion`, and that claim is true only because the equivalence normalizer stripped these fields at every nesting depth — which is why nine prior passes never observed it. Deferred from Story 6.2 as a production behaviour change outside a hosting migration's scope; Story 6.2 discloses the weakness with its measured cause in the AC6 evidence and keeps `occurredAt` participating in the equivalence assertion. A fix must make replay reproduce event-derived timestamps, or the contract must stop calling these fields domain content (`src/Hexalith.Conversations.Server/Projections/ConversationProjectionMaterializer.cs:127`)

- **[HIGH]** Give Conversations a durable, event-fed tenant access projection with freshness and sequence/gap detection, replacing the platform-default `InMemoryTenantProjectionStore` that `AddHexalithTenants` registers when no other `ITenantProjectionStore` is supplied (`references/Hexalith.Tenants/src/Hexalith.Tenants.Client/Registration/TenantServiceCollectionExtensions.cs:84-88`). Conversations registers no replacement, so the tenant access projection is per-replica and non-durable: after a restart, or on a second replica, DAPR never redelivers acked events and that instance's projection stays empty for every pre-existing tenant. Because tenant admission fails closed, the symptom is denial of legitimate callers rather than an error, and one replica can admit a request its sibling denies. `project-context.md:54` assigns the local event-fed tenant access projection to Conversations and `:57` requires production projections to track freshness and sequence/gap behavior, so this is Conversations-owned work, not a platform default to live with. Deferred from Story 6.2 by decision D2 resolution (c) on 2026-07-31 — a durable projection is a feature, not a hosting migration, and folding it into a story already nine review passes deep would widen it again. Story 6.2 discloses a single-replica-only deployment constraint instead; that disclosure must be withdrawn when this lands (`src/Hexalith.Conversations.Server/TenantAccess/ConversationTenantAccessService.cs:22`)

## Deferred from: code review pass 11 of 6-2-migrate-conversations-to-platform-owned-hosting (2026-07-31)

- The AppHost production-boundary lane is load-sensitive at startup. `WaitForResourceHealthyAsync` returning
  does not guarantee the Aspire-assigned endpoint is accepting connections: running the lane immediately
  after the ~4.5-minute IntegrationTests lane reproduced `HttpRequestException: Connection refused
  (localhost:40397)` on a run that took 368 seconds instead of the usual ~60. Measured once, not reproducible
  on demand, so it is disclosed rather than patched — a connect-retry asserted as a fix without a
  demonstration would be the unverifiable strengthening pass 10 removed. A separate DAPR cold-start mode
  (HTTP 500 from `/api/v1/commands` with `Standard-AttemptTimeout` then `Standard-TotalRequestTimeout` and no
  domain-side error, ~1 run in 4) **is** fixed, by a bounded 5xx-only invocation-readiness window.
  Consequence for anyone reading a red run: check both signatures before concluding the production boundary
  regressed. [tests/Hexalith.Conversations.AppHost.Tests/ConversationsAppHostRuntimeBoundaryTest.cs:99]
- The live lanes cannot run while another Aspire application is up on the same machine, and the failure does
  not say so. Measured during pass 11: `ConversationGatewayLiveFixture` failed with `daprd exited with code 1
  during the health check` on two consecutive runs, and `ss -ltn` showed a concurrent session's Aspire `dcp`
  process holding the DAPR defaults **3500**, **50001**, **50101**, **50201**, and **50301**. Nothing in the
  Conversations tree was at fault. The fixture already randomizes its own app ports; what it cannot do is
  share the fixed DAPR control-plane ports. Either the fixture should detect the collision and say so, or the
  lane needs a documented "no other AppHost running" precondition — silently reporting a bare daprd exit code
  sends the reader looking for a projection defect that is not there.
  [tests/Hexalith.Conversations.IntegrationTests/Projections/ConversationGatewayLiveFixture.cs:561]

## Deferred from: code review pass 12 of 6-2-migrate-conversations-to-platform-owned-hosting (2026-08-01)

- **[HIGH] Historical verification does not enforce candidate finality.** Live generation calls
  `evaluate_candidate_binding` and blocks when any non-record-output path or root gitlink changes after the
  recorded candidate. `verify_historical` re-derives only the baseline-to-`file_list_commit` range and never
  applies that check or cross-checks the rendered `### Candidate Binding` section. The failure is reachable:
  the Story 6.2 record at commit `f62a9d0` still named candidate `dd656eeb`, while
  `dd656eeb..f62a9d0` changed the AppHost runtime-boundary test and release evidence, yet historical mode
  returned `pass`. Reapply the committed candidate-finality invariant in historical mode while allowing only
  declared final-record outputs after the candidate, and add a regression covering a later edit to an
  already-listed non-output path. [_bmad/scripts/generate_story_record.py:1359,1806]

- source_spec: none
  summary: Implement Story 7.2 to derive test, path, candidate, submodule, and gitlink facts mechanically.
  evidence: Split from the requested mechanical final-record outcome because v9 defines fact extraction as an independently shippable story after the Story 7.1 generator core.

- source_spec: none
  summary: Implement Story 7.3 to integrate final-record generation into every blocking completion transition.
  evidence: Split from the requested mechanical final-record outcome because v9 gives workflow integration its own predecessor, rollback boundary, acceptance scenarios, and final record.

- source_spec: none
  summary: Implement Story 7.4 to verify historical mode and the complete required fault-injection matrix.
  evidence: Split from the requested mechanical final-record outcome because v9 makes historical verification and non-vacuity proof a separate successor story after workflow integration.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-define-the-final-record-schema-and-deterministic-generator-core.md`
  summary: Implement the additive deterministic v2 generator core, contract-mode CLI, paired JSON/Markdown outputs, and Story 7.1 final record.
  evidence: Split from the schema-contract slice after the 1922-token draft exceeded the workflow limit; published v9 currently couples both scopes, so execution requires an approved authority amendment that preserves the existing obligation mapping.
